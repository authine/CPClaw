# CPClaw 项目进度

本文件是 CPClaw 项目的实时进度看板。之后每完成一个任务、一次验证、一次阻塞排查或一次交付，都先更新本文件，再单独提交并推送到 GitHub，方便远程查看最新状态。

## 当前结论

**项目尚未全部完成。**

2026-07-15 完成“思考过程在上、正式回答在下”的消息层级修复。前端 `ChatView.vue` 在执行中先展示展开的真实思考与执行时间线，Markdown 正式回答紧随其后接收流式增量；执行结束后时间线不再消失，而是转换为默认关闭、可重新展开的“思考与执行过程”折叠区，历史会话恢复后仍保留。真实浏览器复测“系统现在有多少商机？”返回真实对象 `int_bu_oppor`、总数 237；连续追问“这些商机按阶段分布怎么样？”返回未签约 135、暂停 13、已签约 65、停止 20、丢单 4。展开过程后正文始终位于折叠区下方，390px 视口无横向溢出，控制台无错误；前端生产构建通过。当前源码和设计文档仍在本地脏工作区，本文件按约定单独提交并推送。

2026-07-15 智能问数咨询式报告完成第二轮框架修订。系统现在可在宽泛经营问题中从主对象元数据图选择上游和下游对象，分页读取最多 20,000 条真实云枢列表数据，并通过关联表单字段中的业务 ObjectId 验证线索、商机、合同等对象的逐条关系；无法验证关联时不再用同期对象总数相除并冒充转化率。金额、阶段等字段覆盖不足时，KPI 改为“已识别金额合计”等受限口径并显示覆盖数。普通多轮金额合计、阶段分布、年度和人员聚合也提升为全量分页上限 20,000 条，修复 237 条商机只统计前 100 条的问题。流式链路新增统一可感知规则分片，模型流必须收到 `[DONE]` 或有效 `finish_reason` 才算完整，提前 EOF 会触发 `answer_reset` 和规则回退。前端结构化报告移除与 Markdown 重复的结论、风险和建议，只保留 KPI、图表、口径、来源和高相关追问。验证结果：后端全量 15 个测试通过，前端生产构建通过；桌面和 390px 移动视口无报告横向溢出，控制台无错误。后端已使用 MySQL 配置启动在 `http://127.0.0.1:8080/`，前端运行在 `http://127.0.0.1:5173/`。当前仍未全部完成：模型执行规划尚未统一驱动所有运行态过滤/分组/排序，“我的”仍缺正式账号到人员 ObjectId 映射，超过 20,000 条需要云枢服务端聚合。

2026-07-15 真实多轮复测完成。第一轮“2025年上半年商机整体经营情况怎么样？”返回上半年商机 147 条、金额 3973.32 万元、已签约 48 条；思考、规划、逐对象查询、模型正文和模型不完整后的 `answer_reset` 均按 SSE 实时到达。第二轮追问“重新按全部商机计算，其中金额合计是多少？”继承真实商机对象 `int_bu_oppor`，按 237/237 条计算，金额合计 58,069,016.67。第三轮追问“这些商机分别处于什么阶段？”返回未签约 135、暂停 13、已签约 65、停止 20、丢单 4，合计 237 条。QA 临时会话验证后已删除，未留在用户历史列表。

2026-07-15 完成咨询式报告模型输出收尾。真实模型在 8 秒和 20 秒窗口下曾因输出过长触发重置或长度截断，本轮不是继续无限延长等待，而是把报告正文职责收敛为结论、关键依据、风险和建议：分析窗口 20 秒、`max_tokens=1800`、系统 Prompt 要求约 1000 个中文字符并保证结尾完整；模型流的 `finish_reason=length` 明确判定为失败，不能持久化半截回答。最终真实复测“2025年上半年商机整体经营情况怎么样？”约 52.6 秒完成，`reset=0`、`mode=model`，模型正文 962 个中文字符且结尾完整。最终后端全量测试为 16 个，0 失败、0 错误；QA 临时会话均已删除。

2026-07-15 本轮完成从用户输入、意图理解、执行规划、云枢取数到正式回答的端到端真实流式改造。原有 `/api/conversations/messages/stream` 只实时发送少量 `progress`，其余步骤完成后再把完整回答人为切块，属于“完成后伪流式”。本轮建立统一 SSE 事件协议：`thought` 实时展示可审计的理解、规划和校验摘要；`execution` 展示元数据命中、真实 `schemaCode`、云枢接口调用、总数/返回数、数据来源和模型状态；`answer_start / answer_delta / answer_end` 展示正式回答流；`answer_reset` 用于模型输出不完整或超时时清空正文并切换规则兜底；`final` 仅回填持久化消息和完整 Agent 审计信息。模型网关已支持 OpenAI-compatible `stream=true`，直接解析 `choices[0].delta.content` 并转发 token，不再等待完整模型响应后切块。前端同步支持思维步骤与执行结果时间线、执行数据摘要、模型/规则回答增量 Markdown，以及模型超时后的正文重置和兜底提示。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，新增断言覆盖事件顺序及模型分析多段 `answer_delta`；`web/` 下 `npm run build` 通过；真实服务复测“系统有多少商机？”约 3.2 秒完成，事件顺序为 `thought → execution → answer_start → answer_delta → answer_end → final`；“商机都是什么情况？”真实模型先输出 token，模型未在时限内完整结束后正确发送 `answer_reset`，再流式输出 fallback，未把不完整模型正文与规则答案拼接。当前前端运行于 `http://127.0.0.1:5173/`，后端运行于 `http://127.0.0.1:8080/`。

2026-07-03 本轮根据用户截图反馈“右边执行详情展开后，中间主对话区域出现很大空白”，完成主对话布局修复。根因确认：`ChatView.vue` 中 `.chat-shell` 使用 `grid-template-rows: auto minmax(360px, 1fr) auto`，右侧执行详情展开后整体工作台高度被右侧面板撑高，中间消息区被强制拉成剩余高度；同时 `.chat-messages::before { margin-top: auto; }` 会在消息较少时制造额外垂直空白，导致输入框和内容被明显拉开。本轮将主对话区调整为内容优先布局：`.chat-shell` 改为 `grid-template-rows: auto auto auto` 并 `align-content: start`，消息区 `min-height` 改为 0，移除自动顶边占位伪元素，输入区跟随消息内容自然排列。验证结果：`web/` 下执行 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示。当前前端热更新或刷新页面后可验证右侧展开时中间不再出现大块空白。

2026-07-03 本轮继续排查用户反馈“商机都是什么情况？”仍显示“前 10 条记录摘要、名称重复、没有大模型总结”。复盘结论：页面最初仍连着旧后端进程，旧进程启动于 2026-07-02 23:03，早于本轮修复；重新打包时 jar 被旧进程占用，导致新版逻辑没有替换成功。停止旧进程、重新打包并用 `DATABASE_PASSWORD=h3password`、`CPCLAW_VECTOR_ENABLED=true`、`CPCLAW_VECTOR_PASSWORD=h3password` 启动新版后端后，接口验证确认不再返回“前 10 条记录摘要”，也不再出现“名称：北京菲斯曼供热，名称：北京菲斯曼供热”这类重复字段。进一步线程栈排查确认，真实环境慢点在 `OpenAiCompatibleModelGateway.analyzeRecords()` 调用大模型 HTTP，模型服务响应超过同步接口可接受时间；因此本轮新增模型分析硬超时和意图规划硬超时，模型慢或不可用时快速回退到规则业务摘要，避免用户长时间卡住。同步优化宽泛“情况/概况”问题的取数策略：不再按 100 条补详情，而是小样本快速概览；传给模型的运行态记录最多 8 条，并压缩为名称、金额、阶段、负责人、客户、时间等业务字段，减少原始 JSON 和内部结构污染。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过；新版后端已重新打包并启动，`http://127.0.0.1:8080/api/metadata/apps` 返回 200；接口实测“商机都是什么情况？”耗时约 22.8 秒，返回业务概览/阶段分布/金额概览/负责人分布，不再返回旧摘要。当前模型超时场景仍会走规则兜底，后续需要继续把模型总结改造成真正 token 级流式，并在 UI 上明确显示“模型总结中/模型超时后规则兜底”的状态。

2026-07-03 本轮根据用户截图反馈“商机都是什么情况？”回答机械、名称重复、疑似没有经过大模型总结，完成最终回复链路修复与回归验证。根因确认：云枢运行态查询后的普通列表/情况类分支此前会直接拼接“前 N 条记录摘要”，没有统一进入大模型总结；分析类分支也存在规则概览与模型输出混合的问题，导致回答像表格字段转述，不能围绕用户问题组织结论。修复后，查询/分析类最终回答统一优先调用模型总结：用户问题、匹配到的云枢实体、数据项/关系/API 执行上下文、运行态返回记录会一起进入总结链路；只有模型不可用或返回空内容时，才使用稳定规则兜底。同步增强“情况 / 概况 / 整体”等表达的分析识别，避免“商机都是什么情况”落入普通列表摘要；模型调用前和 Agent 返回内容增加敏感信息脱敏，避免 API Key、密码等被提示词或执行详情泄露。回归测试新增“商机都是什么情况？”用例，断言最终意图为 `analyze_data`、候选对象为“商机”、正文包含“结论摘要”，且不再出现“前 10 条记录摘要”和重复名称片段；同时修复测试暴露出的“删除第一条商机”目标识别回归，删除仍要求二次确认，但明确“第一条/第几条/记录 ID”时可以生成确认单并在确认后执行云枢 Delete。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。本轮源代码仍保留在本地待用户验证，本文件将单独提交并推送到 GitHub。

2026-07-02 本轮根据用户截图反馈“张峻千有多少商机？”仍返回商机总数，完成一次人员/负责人筛选意图修复。根因确认：系统此前只识别到“商机 + 多少”并进入计数查询，未把“张峻千”理解为负责人/销售筛选条件，也没有在云枢运行态返回记录上按负责人、销售、业务员等字段过滤，导致回答“商机总计 237 条”，与用户问题不匹配。本轮修复不是硬编码具体姓名，而是在 Agent 槽位层新增负责人/销售筛选提取，右侧思考过程可显示 `负责人/销售=某人`；运行态服务新增通用人员筛选层，支持 `某人有多少商机`、`某人名下有几个项目`、`负责人是某人` 等表达，按 `owner / ownerName / sales / salesName / 负责人 / 销售 / 业务员` 等字段读取人员值并过滤后再执行计数、金额或列表回答。同步修正误判保护：`项目基础数据里的商机有多少`、`每年的客户量情况怎么样` 这类应用路径或维度问题不会被误识别为人员筛选。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，新增回归断言 `张三有多少商机？` 返回 `共 **1** 条` 且不返回 `总计 **237** 条`；`web/` 下 `npm run build` 通过；后端已重新打包并启动，当前 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

2026-07-02 本轮根据用户确认的“参考 Codex/OpenClaw 交互模式改造对话界面”完成主聊天界面阶段性落地。前端 `ChatView.vue` 已将三栏工作台调整为中间对话优先：左侧历史会话与右侧执行详情均支持折叠，折叠后只保留窄操作栏，释放主对话空间；右侧面板名称由“后端处理流程”改为“执行详情”，内容定位为本轮上下文、处理链路和数据来源，不再作为后台流程面板干扰对话主体；助手消息在执行中显示实时“正在思考”状态和步骤时间线，完成后在消息底部保留可展开的“思考完成”摘要，点击消息仍可在右侧查看完整执行详情；右侧新增对象、编码、数据来源、返回数量等上下文摘要，便于验证系统是否基于真实云枢对象执行。验证结果：`web/` 下 `npm run build` 通过；当前前端 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。当前仍未完成生产级模型 token 级流式输出，模型网关仍需后续改造 `stream=true` 解析并把 AI 生成 token 实时转发到前端。

2026-07-02 本轮根据用户反馈“还是没有流式输出效果，需要检验从意图识别到取数和调用 AI 的过程”，完成一次流式链路根因排查和后端执行进度修复。排查结论：此前 `/api/conversations/messages/stream` 只是外层 SSE 包装，Controller 先发送几条固定 progress，然后后台同步执行 `conversationService.sendMessage()`，等“意图识别 -> 元数据匹配 -> 云枢取数 -> AI/规则总结 -> 保存消息”全部完成后，才把最终答案切块返回；因此用户看到的是“执行完后分块展示”，不是 OpenClaw/Codex 风格的执行中流式。修复方案：新增 `AgentProgressListener`，`ConversationService` 支持带进度回调的 `sendMessage(...)`，`AgentOrchestrator` 在真实执行节点推送进度，包括保存用户消息、读取上下文、判断是否承接上一轮对象、识别业务动作和指标、匹配云枢元数据、开始调用云枢运行态接口、云枢数据返回、校验数据来源和保存回答。`ConversationController` 的 SSE 已去掉固定假进度和轮询心跳，改为直接转发 Agent 真实执行节点。实测 `/api/conversations/messages/stream` 已能连续返回真实 progress：理解问题、规划路径、查询数据、整理回答，并在数据返回后再输出正文 chunk 和 final。当前仍未完成的是模型 token 级正文流式：`ModelGateway` 仍使用阻塞式 OpenAI-compatible `/chat/completions`，因此 AI 总结文本还不是边生成边推送；下一阶段需要把模型网关改造成 `stream=true` 的 SSE 解析，并让运行态分析分支把模型 token 转发到前端。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过；后端已重新打包并启动，`http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

2026-07-02 本轮根据用户反馈“参考 OpenClaw 或 Codex 的交互风格，现在仍然很死板”，继续优化对话主界面的 Agent 交互形态，而不是只替换文案。前端聊天内 pending 思考过程已从后台列表改为轻量实时状态块：标题显示“正在思考”和耗时，下面显示当前进度，再用细时间线、状态点和当前步骤高亮展示“理解问题 / 规划路径 / 查询数据 / 整理回答”；消息气泡进一步减弱厚边框和蓝色后台感，助手消息更接近 Codex/OpenClaw 的轻量正文容器。右侧处理流程面板同步改为 Agent 运行摘要：意图与计划合并成顶部胶囊摘要，处理链路变成细竖线时间线，候选对象改为弱分隔列表，减少表单字段堆叠和审计后台感。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示。

2026-07-02 本轮根据用户反馈“不要出现 Observe/Think/Act/Reflect 这些内容，参考 OpenClaw 的思考过程和执行链路过程”，完成可见处理链路的业务化改造。核心调整：后端 `AgentResponse.steps` 新消息统一输出 `理解问题 / 规划路径 / 查询数据 / 整理回答`，步骤摘要从内部调试字段改为面向业务的自然语言，例如“本轮承接上一轮业务对象”“识别为查询/分析任务”“已调用云枢运行态接口读取业务数据”“结果可返回/需补充信息”；前端等待态与右侧流程面板同步改为“处理链路”，并增加旧历史数据兼容归一化，即使历史或 SSE 事件里带有旧工程标签，也会在界面展示为业务化标题；运行态回答正文中的“回答逻辑 / 执行动作”改为“处理口径 / 处理方式”，避免普通回答像调试日志。技术文档和测试用例同步更新，明确内部 `Observe / Think / Act / Reflect` 只允许存在于技术架构或审计结构字段中，不直接展示给最终用户。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。

2026-07-02 本轮根据用户反馈“对话框太土，交互体验不友好，需要改成 OpenClaw 风格”，完成聊天主窗口视觉和交互改造。前端将原先偏后台表单式的输入区重构为现代 Agent Composer：输入框、附件、已选附件、新会话和发送按钮整合在一个圆角悬浮输入面板中；附件组件改为紧凑胶囊按钮，不再占用大块说明区域；消息气泡弱化厚边框和重阴影，改为更轻的左右对话气泡，用户消息靠右、助手消息靠左，宽度、圆角、留白和文本层级重新调整；Markdown 正文字号、标题和列表行距同步收敛，避免回答显得粗糙。现有流式思考步骤列表、复制按钮、右侧处理流程和历史会话能力保持不变。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示。

2026-07-02 本轮根据用户进一步反馈“不能针对哪些商机金额比较高单场景修补，要从系统框架角度解释为什么没有按用户问题回答”，完成一次执行决策层纠偏。根因确认：系统此前只有对象匹配和若干分散运行态分支，缺少“用户问题 -> 指标/维度/排序/筛选/返回形态”的统一 Answer Plan，因此“哪些商机金额比较高”虽然命中了商机对象，但没有识别出 `operation=排行`、`metric=金额`、`sort=降序`，最终掉入普通列表摘要，导致名称重复、没有金额结论、回答与问题不对应。本轮在运行态服务内落地最小可用 `RuntimeQuestionPlan`：将指标、排行/比较、排序方向和 TopN 抽象为回答计划，再由统一排行执行器按计划抽取指标字段、排序、组织结论和 Top 列表；金额排行只是 `metric=AMOUNT` 的一个实例，后续可扩展合同额、收入、项目金额等同类问题。前端同步优化思考过程呈现：pending 助手消息内展示实时 Observe/Think/Act 步骤列表，SSE 正文分块进一步切小，正式回答以更明显的流式追加方式显示。复测中发现真实云枢排行类请求如果默认详情补齐会超过 90 秒，因此同步调整性能策略：排行计划默认轻量列表优先，详情补齐只作为小样本兜底；SSE 外层增加执行期间心跳进度事件，即使云枢接口慢也会持续提示“仍在返回数据/正在整理回答”。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，覆盖“系统有多少商机？ -> 哪些商机金额比较高？”并断言进入 `analyze_data`、继承商机对象、输出金额最高/金额较高结论、不再返回“前 10 条记录摘要”；`web/` 下 `npm run build` 通过。技术方案已同步写入 `docs/technical-design/details/06-key-technical-strategy.md` 第 15.4 节“Answer Plan 回答计划层”。

2026-07-02 本轮根据用户反馈“多轮对话还是不够智能、理解意图慢”和“没有流式输出，查询云枢接口后直接返回结果”，完成意图算法与交互链路的阶段性优化。后端 Agent 接入 `ModelGateway.planIntent(...)` 作为低置信/复杂追问的结构化模型规划兜底，但保留确定性快路径：明确对象继承、金额/阶段/状态/负责人/省份/新老客户等常见聚合不强制调用模型，避免第二轮追问变慢；模型规划只能补充空白或弱槽位，不能覆盖规则已准确识别出的“新老客户”“金额汇总”等高质量维度，也不能编造或覆盖真实云枢 `schemaCode`。同时新增 `POST /api/conversations/messages/stream` SSE 接口，前端对话主链路改为流式读取：先接收 `progress` 更新右侧 Observe/Think/Act 状态，再接收 `chunk` 逐段追加 Markdown 正文，最后用 `final` 替换为持久化后的真实助手消息和完整 Agent Trace。当前流式实现解决“等待期间无反馈、结果整块出现”的体验问题；后续若要做到模型 token 级流式，还需继续改造模型网关和运行态执行管道。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已重新打包并启动新版进程，`http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200；SSE 接口实测返回 `progress`、`chunk`、`final` 事件。技术方案已同步写入 `docs/technical-design/details/06-key-technical-strategy.md` 第 15 章“混合意图规划与流式输出策略”。

2026-07-01 本轮根据用户反馈“测试删除商机，点击确认了，但是还是没有真正执行删除”，完成删除确认后的真实执行链路修复。根因确认：原有确认接口 `POST /api/audit/confirmations/{id}/confirm` 只把确认单状态从 `pending` 改为 `confirmed`，没有继续调用云枢运行态删除接口，因此用户点击确认后只是记录了确认动作，并未删除云枢业务数据。本轮修复后，删除类写操作会在创建确认单前解析可执行计划：必须包含 `appCode`、`schemaCode` 和 `bizObjectId`；如果用户只说“删除商机”而没有明确记录ID或第几条记录，系统会停止并要求补充信息，避免误删；如果用户说“删除第一条商机”或提供记录ID，则系统会先定位记录，创建确认单，用户确认后由确认服务调用云枢对象级 Delete API：`/api/api/runtime/business_rule/{AppCode}/{SchemaCode}/Delete/{bizObjectId}`，HTTP Method 为 DELETE。后端新增 `CloudPivotOperationResult` 和确认后执行器，确认成功后会记录 `cloudpivot_runtime_delete` 审计工具调用，并把确认状态更新为 `executed`。测试专家补充自动化回归：发送“删除第一条商机”后要求二次确认，确认接口返回 `status=executed`、`executed=true`，并用 Mockito 验证 `cloudPivotConnector.deleteRecord(...)` 被真实调用。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。当前仍需重新打包并重启后端后交给用户前端验证。

2026-07-01 本轮根据用户截图反馈“返回的数据用人类的语言进行组装一下，不要直接返回表数据结构”，完成云枢运行态数据可读化修复。根因确认：`CloudPivotRuntimeService` 在单条详情、集合摘要、分析维度中对复杂云枢字段只做了浅层 `String.valueOf()`，当字段值是人员、部门、客户、线索、关联表单等 `Map/List` 结构时，会直接输出 `{schemaCode=...}`、`{excelType=...}`、`{propertyType=...}` 这类内部数据结构。修复后，运行态回答统一通过递归可读格式化处理复杂值：人员/部门优先取 `name`，客户优先取 `cust_fullname`，线索优先取 `sequenceNo`，销售组织优先取 `org_name`，列表字段会合并为“张三、销售一部”这类自然表达，并过滤 `schemaCode`、`propertyType`、`excelType`、`unitType`、`type` 等技术字段；普通摘要由 `key=value` 调整为 `字段：值`，并对 `clues_id`、`sales_org_id`、`ownerDeptId`、`sign_probability_new`、`pre_sign_date` 等常见字段补充中文标签。测试专家同步增强 Mock 云枢商机数据，加入复杂负责人、客户、关联线索、销售组织、所属部门结构；新增回归覆盖“第一条商机的详情说一下”，断言用户正文包含“张三”“北京菲斯曼供热技术有限公司”“QL20250324009026”“华北销售组织”，且正文不包含 `schemaCode=`、`excelType=`、`propertyType=`、`{id=`。同时修复分析路径中的负责人维度提取，避免“分析系统中的商机信息”时负责人分布继续泄露 Map 结构。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。当前改动仍需重新打包并重启后端后交给用户前端验证。

2026-07-01 本轮根据用户截图反馈“前 10 条摘要应该返回整个对象数据，为什么只有 ID”，完成普通列表摘要详情补齐修复。根因确认：真实云枢运行态集合接口在部分对象上只返回记录 `id` 等轻量字段，CPClaw 普通列表查询此前没有启用详情补齐，`recordSummary()` 只能从列表返回体中取字段，因此前 10 条摘要退化成 `id=...`。本轮调整运行态策略：计数问题仍只查总数，避免变慢；但普通列表/情况查询、集合详情展示、单条详情、负责人排行、状态金额分析、宽泛业务分析会按需启用详情补齐，至少让“这些商机是什么情况？”这类问题返回名称、阶段、金额、负责人、创建时间等业务字段，而不是裸 ID。同步修正意图归一化：当规则层判断为普通查询且无明确分析维度时，不允许大模型规划把它升级成 `analyze_data`，避免“这些商机是什么情况？”被误转为业务概览分析；只有“分析一下这些商机的情况”才走宽泛分析。测试专家将 Mock 云枢运行态改为更贴近真实场景：`enrichAllDetails=false` 时商机列表只返回 id，`true` 时返回完整业务字段；新增回归覆盖“系统有多少商机？ -> 这些商机是什么情况？”，断言最终仍为 `query_data`、继承 `商机 / int_bu_oppor`、正文包含“北京菲斯曼供热”和 `stage=方案确认`，且不再是纯 ID 列表。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已停止旧进程、重新打包成功并启动新版 PID=14352；当前 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。用户刷新前端后可重新测试“这些商机是什么情况？”，预期前 10 条摘要展示业务字段而不是只有 ID。

2026-07-01 本轮根据用户复盘“系统有多少商机？返回数量后，再问商机金额有多少，应继承第一次查询到的商机对象并统计金额；再问第一个商机是什么信息，应返回第一条商机详情而不是 ID 列表”，完成多轮商机金额与单条详情自然表达修复。产品口径重新确认：第一轮计数查询只用于确定业务对象和真实 `schemaCode`，第二轮金额问题不能拿第一轮 1 条计数样本直接回答，也不能退回列表；应继承上一轮 `商机 / schemaCode`，重新调用云枢业务对象集合接口获取商机数据，再识别金额字段做 sum/avg/max 聚合。若云枢列表接口未返回金额字段，本轮改为最多补齐前 100 条详情后再尝试金额汇总，不再只补 2 条详情样本；若仍无金额字段，则明确说明当前运行态返回字段不足。单条详情方面，已补充“第一个/第1个/是什么/信息”等表达，`第一个商机是什么信息` 会继承上一轮商机对象，走单条详情路径，按 `schemaCode + 第 1 条记录` 调用详情补齐并输出名称、阶段、金额、负责人、客户、创建时间等字段，不再返回 ID 摘要列表。测试专家新增回归覆盖“系统有多少商机？ -> 商机金额有多少？”和“系统有多少商机？ -> 第一个商机是什么信息”，断言金额问题最终为 `analyze_data`、维度为“金额汇总”、不输出“前 10 条记录摘要”；单条详情问题最终为 `query_data`、维度为“单条详情”、正文包含“第 1 条商机明细”和真实样例名称。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已停止旧 PID=31472、重新打包成功并启动新版 PID=38040；当前 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

2026-07-01 本轮根据用户反馈“执行逻辑还是存在缺陷，第一个对话是查询数据集合，第二个对话是展示详情”，完成“集合查询 / 集合详情展示 / 业务分析”三类意图的边界修正。产品与技术评审结论：`系统有多少商机？` 属于集合计数查询；`分析商机详情`、`展示商机明细`、`列出商机详情` 这类表达虽然可能包含“分析”字样，但核心动作是先查询云枢业务对象数据集合，再展示记录详情，不应直接进入“业务概览、时间分布、建议下一步”等宽泛分析；只有 `趋势 / 分布 / 建议 / 洞察 / 金额汇总 / 负责人排行` 等明确分析维度才进入业务分析。后端已新增集合详情展示分支，并将其放在单条详情之后、聚合分析之前；`AgentOrchestrator` 已把详情/明细/清单/列表/展示类意图识别为 `query_data`，右侧 Think 会展示“动作=查询、分析维度=详情展示”；运行态服务该路径只拉取前 10 条并补齐详情字段，输出名称、记录ID、阶段/状态、金额、负责人、客户、创建时间等字段，不再输出“业务概览/建议下一步”。测试专家新增自动化回归覆盖同一会话中“系统有多少商机？ -> 分析商机详情”，断言继承 `商机 / int_bu_oppor`、最终意图为 `query_data`、正文包含“以下展示前…商机详情”和真实样例“北京菲斯曼供热”，且不包含“业务概览”“建议下一步”“时间分布”。验证结果：`server/` 下执行 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已停止旧 PID=772、重新打包成功并启动新 PID=31472；当前 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。用户可刷新前端后验证“分析商机详情”是否按集合详情展示链路执行。

2026-07-01 本轮根据用户反馈“主对话输出区域不要有内部滚动条，应像 DeepSeek 一样由浏览器滚动查看输出”，完成主消息区滚动行为调整。根因确认：此前 `.chat-messages` 设置了 `max-height` 和 `overflow:auto/hidden` 一类内部滚动策略，长对话时会在对话卡片内部出现独立滚动条，导致用户需要在消息区域内滚动查看输出，不符合类 DeepSeek 的整体页面滚动体验。本轮修复：主工作台 `.chat-workbench` 增加 `align-items:start`，避免三栏等高拉伸；`.chat-shell` 改为自然高度并 `overflow:visible`；`.chat-messages` 移除 `max-height`，改为 `overflow:visible`，让消息内容自然撑开页面，由浏览器/页面滚动条负责查看完整输出。保留最小高度 `180px`，避免空会话过窄；右侧“后端处理流程”作为独立侧栏仍保留自身滚动，主对话输出不再使用内部滚动条。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；当前 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。

2026-07-01 本轮根据用户截图反馈“红色区域的框太高，空白区域太高，导致输入框和内容在很下面”，完成聊天消息区高度二次修正。根因确认：上一轮恢复 `grid-template-rows: auto minmax(0, 1fr) auto` 后，虽然修复了顶部工具栏漂移，但消息区重新占满剩余高度，在空会话或少量消息时会形成过高空白，输入区被压到页面下方。本轮将 `chat-shell` 调整为 `grid-template-rows: auto auto auto` 并设置 `align-content: start`，让工具栏、消息区、输入区按内容贴顶部排列；消息区设置 `min-height: 180px`、`max-height: calc(100vh - 380px)`，既保留必要阅读区域，又避免大面积空白；继续保留 `.chat-messages::before { margin-top: auto; }`，使短内容靠近输入区、长内容仍可滚动。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；当前 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。用户刷新前端即可验证红色区域高度明显收敛，输入框不再被压到很下面。

2026-07-01 本轮根据用户截图反馈“界面乱了，标题、模型选择和会话标题被拉散”，完成聊天工作台布局回归修复。根因确认：上一轮为缩短输入框与消息距离，将 `.chat-shell` 改成 `grid-template-rows: auto minmax(180px, max-content) auto`，在 CSS Grid 有剩余高度时，`auto/max-content` 轨道被拉伸，导致顶部工具栏内部元素被拉到页面中部，模型选择和思考开关漂移，消息区/输入区整体失去稳定三段式结构。本轮修复为：`chat-shell` 恢复 `grid-template-rows: auto minmax(0, 1fr) auto`，保证头部固定、消息区占剩余空间、输入区固定；消息区从 grid 改为 flex column，并用 `.chat-messages::before { margin-top: auto; }` 实现短内容贴近输入区、长对话正常从上到下滚动，避免再次使用会撑坏外层布局的 `max-content` 行高。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；当前 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。用户刷新前端即可验证顶部工具栏和消息区恢复稳定。

2026-07-01 本轮根据用户反馈“第二次问金额时响应时间太长，也没有思考过程输出”，完成金额追问性能与等待态可观测性优化。根因拆解：后端金额汇总此前默认 `queryRecordLimit=20000` 且 `requiresFullDimensionDetails=true`，会按全量详情路径拉取并补全大量运行态记录，导致第二轮金额追问等待过长；前端当前仍是普通 HTTP 请求，不是 SSE/流式协议，因此后端真实阶段不能实时推送到对话框，只能显示本地 pending 占位。修复策略：上下文已继承真实运行态对象且规则层已明确识别分析维度时，Agent 直接走规则快路径，跳过大模型意图规划调用，避免第二轮金额/阶段等明确追问被模型规划拖慢；金额汇总不再默认补全 2 万条详情，改为优先拉取最多 500 条轻量运行态记录聚合金额，只有轻量结果完全识别不到金额字段时才补最多 2 条详情样本，不再逐条详情查询；前端 pending 气泡和右侧流程增加金额追问专属阶段文案，等待期间会显示“正在观察上下文 / 正在理解意图和匹配元数据 / 正在调用云枢运行态接口 / 正在聚合数据并生成回答”，右侧流程会展示“继承上一轮业务对象、识别金额指标、轻量列表聚合金额”的临时思考过程。测试专家新增回归用例覆盖“系统有多少商机？ -> 有多少金额？”省略对象追问，断言第二轮继承 `商机 / int_bu_oppor`、最终意图为 `analyze_data`、分析维度为“金额汇总”、不进入 `clarify_intent`。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已重新打包并重启，当前 PID=772，`http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。真实接口链路用 UTF-8 请求复测：第一轮“系统有多少商机？”约 21.21 秒，第二轮“有多少金额？”约 3.29 秒，第二轮 Observe 显示继承对象=是，有效目标=`int_bu_oppor 商机 有多少金额？`，Think 显示分析维度=金额汇总，Act 显示 total=237、returned=237。当前真实云枢轻量列表返回记录未包含可识别金额字段，因此系统会快速说明“未识别金额字段”，不再长时间逐条补详情；后续若要得到准确金额总额，需要确认云枢商机列表接口是否返回金额数据项，或补充按字段投影/指定查询方案。

2026-07-01 本轮根据用户截图反馈“助手回复显示在上、用户问题显示在下，消息顺序错误”，完成对话消息显示顺序修复。根因判断：历史会话详情接口原本只按 `createdAt asc` 排序，用户消息与助手消息在同一秒或数据库时间精度被截断时，排序可能不稳定；前端也直接按接口返回数组渲染，没有兜底排序，导致同一轮消息可能显示为“助手 -> 用户”。本轮后端将会话消息查询改为显式展示顺序：`createdAt asc`，同时间戳下按 `user -> assistant -> system` 排序，再按 `id asc` 兜底；前端新增 `orderedMessages` 计算属性，渲染前按时间、角色和 id 做兜底排序。测试专家补充回归断言：新建多轮会话后，`GET /api/conversations/{id}` 必须返回第 0 条为用户问题“系统有多少商机？”，第 1 条为助手回复并包含 `agentRunId` 和 `runtime-query`。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已重新打包并重启，当前 PID=30348，`http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

2026-07-01 本轮根据用户截图反馈“对话框距离上面的输出内容太远”，完成对话主窗口布局间距修复。根因是 `ChatView.vue` 中间消息区使用 `grid-template-rows: auto minmax(0, 1fr) auto`，在消息较少时会强制撑满剩余高度，导致输出内容与底部输入区之间出现大面积空白。本轮将聊天主区调整为内容优先排列：消息区从强制 1fr 改为 `minmax(180px, max-content)`，同时设置 `max-height: calc(100vh - 380px)` 保持长对话可滚动；消息区间距从 16px 收紧到 12px，底部 padding 从 20px 收紧到 12px；输入区 padding 从 `16px 20px 20px` 收紧为 `12px 20px 16px`，内部 gap 从 12px 收紧为 10px。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；`http://127.0.0.1:5173/` 返回 200，`http://127.0.0.1:8080/api/metadata/apps` 返回 200。用户刷新当前前端即可验证输入框是否更贴近上一条输出。

2026-07-01 本轮针对用户截图反馈“商机多少金额？”仍返回“总计 237 条”的错误，完成金额聚合可执行路径最小闭环修复。评审结论确认：该问题不应被当作商机数量查询，而应理解为“基于上一轮商机对象，查询商机数据集合，识别金额类数据项，执行金额聚合，再返回大模型/规则总结”。本轮实现包括：Agent 层新增金额聚合意图识别，`多少金额 / 多少钱 / 金额合计 / 总额 / 汇总` 等表达会进入 `analyze_data`，分析维度为“金额汇总”；运行态层新增金额聚合分支，并将其放在计数分支之前，避免被 `多少` 截走；执行层对金额聚合使用云枢业务对象数据集合查询接口，不再只取 1 条计数样本，而是按聚合任务拉取更多记录并识别 `amount / 金额 / 商机金额 / 预计金额 / 合同额 / 收入` 等金额字段，输出可识别金额记录数、金额合计、平均金额、最大单笔金额和回答逻辑。新增自动化回归覆盖同一会话中“系统有多少商机？ -> 商机多少金额？”链路，断言继承 `商机 / int_bu_oppor`、最终意图为 `analyze_data`、分析维度为“金额汇总”、正文包含“商机金额合计：**1540000**”且不返回“前 10 条记录摘要”。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；完整 `mvn test` 通过，结果为 Tests run: 10, Failures: 0, Errors: 0, Skipped: 0。后端已重新打包并重启，当前 PID=2688，`http://127.0.0.1:8080/api/metadata/apps` 返回 200；前端 `http://127.0.0.1:5173/` 返回 200。

2026-07-01 本轮针对用户反馈“Think 理解意图仍不智能，第二轮应承接上一轮商机继续分析”完成结构化意图规划第一版增强。核心策略调整为：先由规则层识别安全边界和确定性意图，再调用模型规划器输出结构化意图 JSON，包括动作、业务对象、分析维度、字段线索、关联线索、推理说明、是否需要澄清和置信度；最后用真实云枢元数据执行计划进行校验，禁止模型编造 `schemaCode` 或把写操作降级为查询。宽泛追问如“分析一下这些商机的情况”会继承上一轮真实 `商机 / int_bu_oppor`，识别为 `analyze_data`，并展示业务概览、阶段/状态分布、金额概览、负责人分布、时间趋势、关联客户分析等建议维度；明确查询如“查询系统商机情况”仍保持查询；“返回第一条商机明细”仍保持单条明细查询；“谁的商机最多”保留规则层更具体的“销售/负责人 + 金额/数量”维度。安全修复同步完成：当规则层识别为新增、修改、删除或附件填单等高风险动作时，模型规划不能将其降级为普通查询，仍按二次确认链路处理。右侧 `Think` 摘要已补充“建议分析维度”，并压缩元数据链路，避免关键推理信息被长文本挤掉。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；完整 `mvn test` 通过，结果为 Tests run: 10, Failures: 0, Errors: 0, Skipped: 0。后端已重新打包并用 MySQL/pgvector/Embedding 配置重启，当前 PID=29548，`http://127.0.0.1:8080/api/metadata/apps` 返回 200；前端 `http://127.0.0.1:5173/` 返回 200。

当前更准确的状态是：团队已完成一次关键纠偏并通过真实云枢环境验收：CPClaw 的业务问答不再允许使用 `system_opportunity`、`system_customer` 等本地演示编码冒充真实云枢 `schemaCode`。新的主线验收口径是：用户自然语言 -> `Think` 阶段抽取动作、业务对象、维度和筛选条件 -> 从已同步的真实云枢 Metadata Index 命中应用/模型 -> 使用真实 `schemaCode` 调用云枢运行态接口 -> 将真实返回数据交给规则聚合或大模型分析 -> 输出结论。没有真实元数据或运行态查询失败时，系统必须解释原因并停止生成业务结论，不能编造数量或分析结果。

本轮后端已落地对应保护并完成真实复测：移除 fallback 中的“系统商机/系统客户”演示业务模型和演示运行态数据；运行态服务遇到 `local-fallback` 会拒绝生成业务答案；元数据检索改为业务词全量候选召回后统一排序，避免正确 CRM 模型被前 10 条截断；Agent 的 `Think` 阶段会展示动作、对象、维度、筛选和候选编码；新增自动化测试验证真实样例 `int_bu_oppor` / `crm_customer` 可执行，同时验证 fallback 环境下“系统有多少商机？”不会返回假 `schemaCode` 或假总数。真实云枢复测结果：同步 29 个应用、922 个模型、951 条搜索文档；“系统有多少商机？”命中 `商机 / int_bu_oppor` 并返回 total=237；“系统有多少个客户？”命中 `客户 / crm_customer` 并返回 total=8694；“每年的客户量情况怎么样？”命中 `crm_customer` 并拉取 returned=8694 后按年分析。本轮新增对象级多轮上下文继承：同一会话中先问商机数量、再追问“分别在什么阶段？”时，系统会继承上一轮真实运行态对象和 `schemaCode`，重新查询云枢运行态并按阶段聚合；显式应用路径下的同名对象也会继承上一轮真实 `schemaCode`，避免跳回默认 CRM 对象。真实代理复测中，追问“分别在什么阶段？”继承 `int_bu_oppor` 并返回阶段分布：未签约 135、暂停 13、已签约 65、停止 20、丢单 4。

最新一轮已按用户提供的向量库和 Embedding 服务策略完成混合元数据检索真实初始化与双 QA 验证：MySQL `metadata_search_documents` 仍是权威 Metadata Index，PostgreSQL pgvector `metadata_vector_documents` 作为可重建语义增强索引；OpenAI-compatible Embedding 客户端用于生成查询向量和索引向量。初始化排查发现，最初一次性发送 965 条文本会导致 Embedding 服务返回空向量；改为批处理后又发现部分 32 条批次会被服务以 400 拒绝。本轮已将 Embedding 客户端调整为小批量请求并在 400/413 时递归拆小重试，保持返回向量与原始元数据文档下标一致。真实重建后，pgvector 向量表已写入 965/965 条，维度统一为 1024，分布为 app=29、entity=936，模型为 `text-embedding-v4`。向量召回只对真实元数据文档加有限分，不能生成或猜测 `schemaCode`，也不能覆盖编码精确、名称精确、显式应用路径或上一轮真实 `schemaCode` 继承。当前仍未完成的是生产级完整智能体：模型驱动结构化意图、完整字段/关系/Action 图谱、DAG 多工具循环、完整记录级上下文 ID 引用、阶段/状态统计性能优化、向量索引后台重建任务和真实云枢环境的持续回归仍需下一阶段增强。

本轮继续针对用户反馈“多问几次又卡住了”完成运行态连续追问性能稳定化：新增“新客户/老客户/新老客户”分析维度识别和上下文继承词，用户在同一会话中先问客户数量，再追问“新客户多还是老客户多”时会继承 `客户 / crm_customer`，直接进入新老客户字段识别与聚合；若真实返回记录缺少 `customerType`、`客户类型`、`新老客户`、`是否新客户` 等字段，系统会快速说明缺字段，不再盲扫全量。进一步排查发现，后续“阶段/状态”和“列表”追问仍会因为默认详情补取和阶段全量详情聚合而卡顿；本轮已将运行态读取改为轻量列表优先、详情按需小样本补取、慢路径限流：普通列表只返回 10 条且不补详情，省份/阶段/新老客户优先取 100 条轻量样本，阶段字段列表中不可见时最多补 2 条详情样本，按年分析保留最多 20,000 条全量聚合策略。验证结果：`server/` 下完整 `mvn test` 通过，结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0；真实服务两名 QA 并行连续 5 轮追问均未超时，客户链路最慢约 2.6 秒，商机链路最慢约 2.5 秒，列表追问约 0.5 秒，且每轮都继承正确真实 `schemaCode`。当前后端已重启为新版，监听 `http://127.0.0.1:8080/`，前端仍监听 `http://127.0.0.1:5173/`。

本轮针对用户反馈“第二轮对话不应这么慢”完成客户省份追问快路径优化：当同一会话中上一轮已查询到真实运行态对象，例如 `客户 / crm_customer`，用户再追问“这些客户都属于哪些省份？”时，`Observe` 会继承上一轮真实 `schemaCode`，`Think` 阶段跳过重复元数据召回，直接进入省份/区域维度分析；运行态服务新增省份、地区、区域、城市、地址等字段识别和聚合分支，明确统计类追问不再走大模型长分析；云枢批量查询在非全量详情模式下不再默认每页补详情，减少大批量列表查询的多余网络请求。测试专家已补充自动化用例覆盖“系统有多少个客户？ -> 这些客户都属于哪些省份？”同会话追问，验证继承对象为是、命中 `crm_customer`、输出按省份分布且不跳到 `system_customer` 或 `Test009`；`server/` 下定向 `CpClawApiTests` 与完整 `mvn test` 均通过，完整结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0。真实服务复测显示：第一轮客户计数约 1.9 秒，第二轮省份追问约 0.66 秒，`Act` 命中 `schemaCode=crm_customer` 且 `returned=200`，由于当前云枢客户列表样本未返回可识别省份/区域字段，系统快速返回缺字段说明，不再全量翻页等待。

本轮根据用户要求“重新初始化云枢数据模型”，完成实体模型、数据项和关联表单关系同步链路修复，并已在真实云枢环境连续完成初始化验证。关键修复包括：连接器优先调用真实云枢设计态数据项接口 `/api/app/bizproperty/list`，兼容分页/搜索式数据项接口和运行态 `get_biz_schema`；云枢元数据原始配置 `raw_json` 扩展为 `LONGTEXT`，避免真实数据项配置超过 MySQL `TEXT` 上限；关联关系识别删除“全文扫描实体编码”的宽松兜底，只允许从明确目标模型配置的关联表单/关联引用数据项生成关系。最新一次按用户要求重新触发 `/api/metadata/sync` 成功，返回 `syncId=588cb907-997a-4b33-bb8b-d18674743956`、`appCount=29`、`entityCount=940`、`dataItemCount=16868`、`relationCount=936`、`searchDocumentCount=18773`；搜索文档分布包含 `app=29`、`entity=940`、`data_item=16868`、`relation=936`，关系样例可通过 `source_data_item_id` 回查来源数据项并指向真实目标实体。验证已执行 `server/` 下 `-Dtest=CpClawApiTests,MessageStorageTests test`，结果为 Tests run: 5, Failures: 0, Errors: 0, Skipped: 0；当前后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

本轮根据用户反馈“返回第一条商机的明细信息却返回前 10 条列表”，完成单条记录明细意图修复。根因是系统只把“第一条”作为普通查询词或写操作上下文词，没有把“第一条 + 明细/详情/信息”识别为单条运行态记录详情请求，导致运行态默认按列表查询取前 10 条摘要。修复后，同一会话中用户先问“系统有多少商机？”，再追问“返回第一条商机的明细信息”时，`Observe` 会继承上一轮真实 `商机 / int_bu_oppor` 对象，`Act` 只取第 1 条并启用详情补齐，回答只展示该商机的记录 ID、名称、阶段、金额、负责人、创建时间等业务字段，不再返回前 10 条 id 列表。已补充自动化回归用例覆盖该链路，`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已重新打包并重启，当前 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。

本轮根据用户反馈“谁的商机最多应结合商机实体对象和问题给 AI/系统推理，按金额汇总分析哪个销售手上的商机最多”，完成负责人维度商机汇总分析修复。根因是 `Think` 阶段没有把“谁的/哪个销售/最多”识别为分析类问题，也没有把隐含维度推断为“销售/负责人 + 金额/数量”，运行态服务因此按普通查询返回列表。修复后，系统会将“谁的商机最多？”识别为 `analyze_data`，继承上一轮真实 `商机 / int_bu_oppor` 对象，查询运行态商机数据并按销售/负责人字段聚合，同时统计商机数量和金额合计；回答会明确给出“按金额最高”和“按数量最多”的销售，并输出排名前 5，不再返回前 10 条记录摘要。已补充自动化回归用例覆盖该链路，`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端已重新打包并重启，当前 `http://127.0.0.1:8080/api/metadata/apps` 与前端 `http://127.0.0.1:5173/` 均返回 200。

本轮根据用户要求“完整测试，包含多轮对话测试，系统现在有多少项目 -> 进行中的项目有多少，项目金额有多少”，完成项目对象多轮上下文与状态金额聚合专项修复。测试专家先补充项目实体、项目状态、项目金额等云枢元数据模拟和运行态样本，再新增端到端多轮用例：第一轮“系统现在有多少项目？”命中真实元数据对象 `项目 / project_info` 并返回总计 3 条；第二轮“进行中的项目有多少，项目金额有多少？”继承上一轮项目对象，不重新漂移到商机或客户，识别为 `analyze_data`，分析维度为“状态筛选 + 金额汇总”，按项目状态筛选“进行中”并聚合数量 2 个项目、金额合计 1600000。实现上新增状态金额聚合分支，识别 `status/projectStatus/stage/状态/项目状态` 和 `amount/projectAmount/contractAmount/金额/项目金额` 等字段，回答中保留业务结论和“回答逻辑”，执行细节继续放在 Agent 步骤与审计中。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，完整 `mvn test` 也通过，结果为 Tests run: 10, Failures: 0, Errors: 0, Skipped: 0；后端待本轮重新打包重启后交给用户前端验证。

本轮根据用户要求“更智能一些，能根据实体、字段、关联关系推断意图，找到业务链路，调用产品接口获取数据，让 AI 总结返回”，完成元数据执行链路规划器第一版。新增 `MetadataExecutionPlanner`，在 Agent `Think` 阶段把搜索召回结果进一步解析为可执行实体、字段线索和关联关系线索：如果召回的是实体，则直接作为运行态查询对象；如果召回的是数据项，则回溯所属实体并把该字段作为筛选、分组或指标候选；如果召回的是关联关系，则选择来源实体作为执行入口，并把关联表单链路写入推理摘要。Agent 的右侧 `Think` 步骤、审计 `planJson` 和 `metadata_search` 工具输出现在会包含“元数据链路”“字段线索”“关联线索”，从而能看到系统如何从云枢实体模型、数据项、关联关系推断执行任务。自动化测试新增字段驱动和关系驱动场景：`按商机金额分析系统商机` 会识别金额字段 `amount`，`分析商机关联客户情况` 会识别 `商机 -> 客户` 关联链路且仍以 `int_bu_oppor` 作为可执行入口。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，完整 `mvn test` 通过，结果为 Tests run: 10, Failures: 0, Errors: 0, Skipped: 0。

本轮按用户要求启动 2 个测试专员，参考现有云枢应用能力编写专项测试用例并完成基础验证。QA-A 负责元数据能力测试，覆盖 `zlcsstcrm`、`ProjectInitiation`、`pmo_01`、`taskSystem/taskSystemNew`、`hrm` 等应用的实体、字段、关联关系召回；QA-B 负责对话链路测试，覆盖多轮上下文承接、字段线索展示、关联链路推断、歧义澄清和审计可观测性。新增正式测试文档 `docs/test-cases/12-metadata-business-chain-validation-2026-07-01.md`，沉淀 25 条测试用例和执行说明。已执行验证包括：`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；真实 `/api/metadata/search` 抽样验证显示“商机金额”命中 `entity / int_bu_oppor`，“商机关联客户”命中 `data_item / cust_id`，“项目状态”命中 `data_item / project_status`，“PMO 项目”命中 `entity / project`，“客户省份”命中 `entity / crm_customer`。剩余前端多轮和审计面板用例已列入待人工验证清单。

## 当前快照

- 日期：2026-07-01
- 分支：`main`
- 远程仓库：`origin` -> `ssh://git@github.com/authine/CPClaw.git`
- 当前阶段：MVP 稳定化、结构化意图规划增强、真实云枢元数据模型同步增强与联调回归
- 当前主目标：交付一个可用的云枢超级智能体对话式工作台，覆盖设置、元数据初始化、历史会话、附件、确认和审计入口。
- Git 状态：本轮已完成主对话输出区内部滚动条移除，`.chat-shell` 和 `.chat-messages` 改为自然高度与 `overflow:visible`，由浏览器页面滚动查看完整输出，保留右侧流程面板自身滚动。`web/` 下 `npm run build` 通过；当前前端 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。当前仍有多处前序任务本地未提交改动，提交时需继续避免混入无关文件；本次只更新并推送 `PROGRESS.md`。

## 原始需求基线

CPClaw 是面向云枢低代码平台的对话式超级智能体。用户应能通过自然语言访问和操作云枢应用，就像真实员工通过浏览器或 API 使用系统一样。

MVP 核心要求：

- 对话式 UI 是第一入口。
- 支持多轮对话。
- 支持新建会话。
- 支持历史会话保存和查看。
- 支持系统设置：云枢登录地址、账号、密码、模型配置。
- 支持管理员初始化云枢应用架构元数据。
- 使用已同步的真实云枢 Metadata Index 做意图匹配和能力发现；本地 fallback 只能用于连接/流程自测，不能作为业务问答依据。
- 采用 ReAct + Reflection 的智能体设计方向；当前已落地 MVP 执行链，后续继续增强模型驱动结构化意图、DAG 多工具循环和完整上下文引用。
- 优先落地查询类场景。
- 新增、修改、删除、流程、Action、附件等高风险操作必须二次确认。
- 审计记录和工具输入输出必须脱敏。

## 完成情况总览

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 中文进度文件 | 已完成 | 已新增本文件，并已推送到 GitHub。 |
| SubAgent 协作看板 | 已完成 | 已新增 `SUBAGENTS.md`，维护产品、项目、UI、前端、后端、QA 六个角色的职责、状态、任务和阻塞。 |
| 团队测试轮 | 已完成本轮 | 本轮新增两个测试专家并行验证：QA-A 验证 MySQL/pgvector/混合检索链路通过，QA-B 验证前端可访问、真实业务问答、多轮上下文和右侧流程审计恢复通过。 |
| 智能体专项测试 | 已完成本轮 | QA 已执行 14 条自然语言智能程度用例，13 条严格验收全部通过，1 条复杂上下文用例验证 ReAct + Reflection 安全澄清能力。 |
| 对话主界面 | 本地已实现，待提交 | 已补历史会话、新建会话、模型选择、思考模式、附件入口和确认入口；普通对话默认只展示业务结果。 |
| 设置页 | 本地已实现，保存链路已通过临时后端联调 | 已补普通用户云枢账号、管理员元数据环境、模型配置和连接测试入口；后端未启动时保存会提示后端服务不可用；后端启动后设置保存经 `5173` 前端代理返回 200。 |
| 元数据页 | 本地已实现，待提交 | 已恢复路由入口，并补同步、搜索、加载、错误和结果摘要。 |
| 云枢实体模型/数据项/关联关系同步 | 已完成本轮真实初始化 | 已从真实云枢同步 29 个应用、940 个实体模型、16868 个数据项和 936 条关联表单关系；最新一次同步 `syncId=588cb907-997a-4b33-bb8b-d18674743956`；`metadata_search_documents` 已生成 `data_item=16868`、`relation=936`，不再只有应用和实体两层。 |
| 审计页 | 本地已实现，待提交 | 已恢复路由入口，并补 Agent Run 查询、空态、未找到态、加载态和脱敏工具调用表格。 |
| 前端构建 | 已通过 | `npm run build` 已通过。 |
| 前端路由检查 | 已通过 | `/`、`/settings`、`/metadata`、`/audit` 均返回 200。 |
| 敏感信息检查 | 已通过本轮 diff 检查 | 未发现原始云枢账号密码、明显 Bearer Token 或 API Key 字面量。 |
| 后端自动化测试 | 已通过 | 本轮针对“项目多轮追问状态金额聚合”补充回归用例，`server/` 下 `-Dtest=CpClawApiTests test` 通过；完整 `mvn test` 通过，结果为 Tests run: 10, Failures: 0, Errors: 0, Skipped: 0。 |
| 完整前后端联调 | 部分完成 | 当前已使用 MySQL `CPClaw` 持久化库启动后端并经 `5173` 前端代理验证商机计数与阶段追问；设置保存、模型新增、管理员环境保存等生产持久化链路仍需专项复测。 |
| 分析类对话意图 | 已完成本轮纠偏 | “分析系统中的商机信息”应识别为 `analyze_data`，但只有命中真实云枢元数据中的商机模型并成功查询运行态数据后才生成分析；未命中真实对象或查询失败时停止生成业务结论。 |
| 对话式澄清 | 已完成本轮修复 | 当意图或云枢对象不明确时返回 `clarify_intent`，说明已理解部分、缺失信息和可选补充方向，不再只回复“没理解”。 |
| 明确统计查询 | 已通过真实云枢验证，待用户验证 | “系统有多少商机？”识别为计数查询，在真实云枢 Metadata Index 中命中 `商机 / int_bu_oppor`，运行态 total=237；fallback 环境不再返回 `system_opportunity` 或假总数。 |
| 客户计数查询 | 已通过真实云枢验证，待用户验证 | “系统有多少个客户？”识别为客户计数查询，在真实云枢 Metadata Index 中命中 `客户 / crm_customer`；本轮真实运行态复测 total=8711；fallback 环境不再返回 `system_customer` 或假总数。 |
| 按年客户量分析 | 已通过真实云枢验证，待用户验证 | “每年的客户量情况怎么样？”识别对象=客户、维度=年份；真实云枢查询命中 `crm_customer`，total=8694、returned=8694 后按年聚合分析。 |
| 多轮对象级上下文继承 | 已完成本轮并通过自动化与真实代理验证，待用户验证 | 同一会话中“系统有多少商机？”后追问“分别在什么阶段？”会继承上一轮真实商机对象和 `schemaCode`，重新查询运行态并返回阶段分布；本轮新增“返回第一条商机的明细信息”单条详情追问，继承 `int_bu_oppor` 后只取第 1 条并补齐详情字段，不再返回前 10 条列表。 |
| 负责人维度商机汇总 | 已完成本轮修复并通过自动化验证，待用户验证 | “谁的商机最多？”会被识别为分析类任务，继承上一轮 `商机 / int_bu_oppor`，按销售/负责人聚合商机数量和金额合计，输出金额最高和数量最多的销售及排名前 5。 |
| 项目状态金额追问 | 已完成本轮修复并通过自动化验证，待用户验证 | 同一会话中先问“系统现在有多少项目？”，再追问“进行中的项目有多少，项目金额有多少？”时，Agent 会继承上一轮 `项目 / project_info`，识别“进行中”为状态筛选、“项目金额”为金额聚合指标，返回进行中项目数量和金额合计，并说明回答逻辑。 |
| 元数据业务链路推断 | 已完成第一版并通过自动化验证，待用户验证 | 新增元数据执行规划器，可把实体、数据项、关联关系召回结果统一转成可执行实体，并在 Think/审计中展示字段线索和关联线索；字段命中可回溯所属实体，关联命中可形成 `来源实体 -> 目标实体` 业务链路。 |
| 元数据链路专项测试 | 已完成用例设计与基础验证，待前端人工验证 | 已由 QA-A/QA-B 两个测试专员输出 25 条用例，覆盖 CRM、项目管理、PMO、任务、HR 等现有云枢应用能力；新增 `docs/test-cases/12-metadata-business-chain-validation-2026-07-01.md`，并完成自动化回归和 `/api/metadata/search` 抽样验证。 |
| 客户省份追问快路径 | 已完成本轮并通过自动化与真实服务复测，待用户验证 | 同一会话中先问“系统有多少个客户？”，再追问“这些客户都属于哪些省份？”时，Agent 会继承上一轮 `客户 / crm_customer`，跳过重复元数据召回，直接按省份/区域字段聚合；运行态批量查询非全量详情模式不再每页补详情，省份追问先限量探测 200 条样本，真实服务复测第二轮约 0.66 秒返回；新增 `CpClawApiTests` 覆盖该多轮场景。 |
| 连续多轮追问性能稳定化 | 已完成本轮并通过双 QA 真实压测，待用户验证 | 新增新老客户维度识别和继承词，运行态查询改为轻量列表优先、详情按需小样本补取、慢路径限流；普通列表只取 10 条不补详情，省份/阶段/新老客户优先 100 条样本，阶段列表字段缺失时最多补 2 条详情样本；QA-A/QA-B 各 5 轮连续追问均未超时，最慢约 2.6 秒。 |
| 消息长文本持久化 | 已完成本轮修复并通过真实复测 | 修复第二轮追问时 `messages.content` 超过 MySQL `TEXT` 上限导致 `Data too long for column 'content'` 的问题；`messages.content`、`messages.metadata_json`、Agent 审计与工具调用长文本字段已扩为 `LONGTEXT`，Flyway `V2` 已应用到本机 MySQL `CPClaw`。 |
| 向量语义检索增强 | 已完成真实初始化并通过双 QA 验证 | 已新增 OpenAI-compatible Embedding 客户端、PostgreSQL pgvector 元数据向量索引、同步时容错写入、查询时向量召回和混合排序；本轮修复大批量 Embedding 请求失败问题，真实 pgvector 表已写入 965/965 条，维度统一 1024；向量候选必须回到真实 MySQL Metadata Index，不能生成或猜测 `schemaCode`。 |
| 执行过程与回答展示 | 已完成并通过本地验证 | 普通对话正文不再展示 `### 执行过程`、真实 `schemaCode`、原始数据摘要和接口细节；这些信息保留在 Agent 步骤、审计和工具调用记录中。 |
| 长耗时任务等待态 | 已完成本轮前端修复并通过构建验证 | 第二轮阶段分布等真实云枢查询可能耗时较长；前端已在发送后立即插入 CPClaw 处理中占位回复，并在右侧处理流程显示临时 Observe、Think、Act、Reflect，后端返回后替换为真实回答和真实步骤。 |
| 右侧处理流程点击恢复 | 已完成本轮修复并通过构建/测试验证 | 新助手消息会在 `metadataJson` 中持久化 `agentRunId`；点击当前或历史 CPClaw 回复时，前端先使用内存 trace，若不存在则读取 `agentRunId` 调用 `/api/audit/agent-runs/{id}` 恢复 Observe、Think、Act、Reflect；旧运行态 metadata 可展示有限摘要，完全无 metadata 的历史消息会提示无法恢复。 |
| Markdown 消息渲染 | 已完成并通过构建验证 | `MarkdownMessage` 已改为解析并安全渲染 Markdown，支持标题、列表、加粗、代码和表格；历史消息中旧版 `### 执行过程` 前置块会在展示层隐藏。 |
| 设计文档同步规则 | 已完成本轮更新 | 已在 `docs/README.md` 明确所有产品、交互、Agent、接口、数据、测试和流程变更必须同步文档；并更新产品需求、产品规划、Agent 设计和 MVP 测试用例。 |
| 关键技术策略文档 | 已完成本轮更新 | 已继续补充 `docs/technical-design/details/06-key-technical-strategy.md`，固化配置持久化、元数据驱动意图匹配、确定性检索 + 向量语义召回混合方案、澄清收敛、流式输出和真实数据原则。 |
| 混合元数据检索 | 已完成真实联调并通过测试 | 已在原有精确名称、别名、编码、应用路径、显式应用路径优先、全文关键词、复合业务词和辅助对象降权基础上接入 pgvector 语义召回；`server/` 下 `mvn test` 通过，结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0；自然语言“帮我看看销售进展池子”可通过向量语义召回命中销售相关对象。 |
| ReAct + Reflection Agent | MVP 已落地 | 后端已拆分 Observe、Think、Plan、Act、Reflect 执行链，响应步骤和审计均可追踪；生产级模型驱动 DAG、多工具循环和完整上下文记录引用仍未完成。 |
| 浏览器自动化兜底 | 未完成 | 设计文档有目标，当前不是完整生产级实现。 |
| 字段级元数据、Action、流程、表单完整同步 | 部分完成 | 当前已有元数据同步 MVP，但不是完整设计态能力采集。 |
| 附件解析与字段映射 | 未完成 | 当前已接上传入口，解析和字段映射仍待实现。 |

## 最近工程进展

### 谁的商机最多分析修复

- 状态：已完成代码修复、自动化回归、后端重新打包和服务重启，待用户在前端验证。
- 用户反馈：“谁的商机最多？”不应返回列表，应拿商机实体对象和用户问题一起推理，理解为按销售/负责人聚合，并结合金额汇总分析。
- 根因定位：`AgentOrchestrator` 没有把“谁的/哪个销售/哪个负责人/最多/最高”等表达识别为分析类问题；`analysisDimension` 没有推断“销售/负责人 + 金额/数量”；`CloudPivotRuntimeService` 缺少负责人维度聚合分支，因此默认走普通查询列表。
- 已处理：新增负责人商机排行意图识别，覆盖“谁的商机最多”“哪个销售商机最高”“哪个负责人商机最多”等表达；同一会话追问时继承上一轮真实 `schemaCode`；运行态服务按 owner、ownerName、sales、负责人、销售、销售人员等字段识别销售/负责人，并按 amount、opportunityAmount、金额、商机金额、合同额等字段汇总金额。
- 输出策略：回答明确区分“按金额最高”和“按数量最多”，并输出排名前 5；如果缺负责人或金额字段，会说明缺失数量和影响范围，不再把结果伪装成完整结论。
- 测试结果：新增 `CpClawApiTests` 回归断言，覆盖 `intent=analyze_data`、继承对象为是、分析维度为“销售/负责人 + 金额/数量”、正文包含“按销售/负责人汇总”“按金额最高：销售一部”“按数量最多：销售一部”，且不包含“前 10 条记录摘要”；执行 `server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 服务状态：后端已重新打包并重启，监听 `http://127.0.0.1:8080/`；前端 `http://127.0.0.1:5173/` 返回 200。

### 第一条商机明细误走列表查询修复

- 状态：已完成代码修复、自动化回归、后端重新打包和服务重启，待用户在前端验证。
- 用户反馈：在同一会话中输入“返回第一条商机的明细信息”，系统返回“前 10 条记录摘要”并只展示 id 列表，不符合“第一条明细”的真实意图。
- 根因定位：`AgentOrchestrator` 的上下文继承词只覆盖阶段、状态、省份、新老客户等维度追问，没有覆盖“第一条/第1条/明细/详情”；`CloudPivotRuntimeService` 对非统计、非分析查询默认按列表查询处理，`pageSize=10` 且不补详情。
- 已处理：新增单条记录明细意图识别，覆盖“第一条/第1条/首条/第一笔/第一单 + 明细/详情/详细/信息/返回/查看”等表达；该类请求 `pageSize` 和 `recordLimit` 均按目标序号收敛为 1、2 或 3，并启用详情补齐；回答只输出目标记录的业务字段，包括记录 ID、名称、阶段、金额、负责人、客户、创建时间和少量额外非敏感字段。
- 上下文修复：同一会话中先问“系统有多少商机？”，再追问“返回第一条商机的明细信息”时，会继承上一轮真实 `商机 / int_bu_oppor`，不会重新漂移到客户或其他同名对象。
- 测试结果：新增 `CpClawApiTests` 回归断言，覆盖继承对象为是、命中 `int_bu_oppor`、`returnedRecords=1`、正文包含“第 1 条商机明细”和“名称：北京菲斯曼供热”、正文不包含“前 10 条记录摘要”；执行 `server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 服务状态：后端已重新打包并重启，监听 `http://127.0.0.1:8080/`；前端 `http://127.0.0.1:5173/` 返回 200。

### 本地服务启动恢复

- 状态：已完成，本机可访问。
- 后端：已使用 MySQL `CPClaw` 配置启动 Spring Boot 服务，监听 `http://127.0.0.1:8080/`；接口探活 `/api/settings` 返回 200。
- 前端：已启动 Vite 开发服务，监听 `http://127.0.0.1:5173/`；首页返回 200。
- 日志位置：后端日志写入 `logs/backend.out.log` / `logs/backend.err.log`，前端日志写入 `logs/frontend.out.log` / `logs/frontend.err.log`。
- 当前说明：本次仅恢复本地运行环境，没有新增业务代码；工作区仍保留若干前序未提交 UI/文档改动，未混入本次服务启动记录。

### 云枢实体模型、数据项与关联表单关系同步增强

- 状态：已完成本轮代码实现、文档同步、自动化验证和真实云枢重新初始化；当前真实同步结果为 29 个应用、940 个实体模型、16868 个数据项和 936 条关联表单关系。
- 产品口径：不再把云枢模型下的信息泛化为“字段拆分”。CPClaw 当前同步的核心对象明确为云枢实体模型、云枢数据项，以及由“关联表单”数据项推导出的实体关联关系。
- 后端实现：`CloudPivotMetadataSnapshot` 已扩展 `dataItems` 与 `relations`；云枢连接器会在实体模型同步后探测业务模型详情接口，从 `dataItems`、`properties`、`fields`、`bizProperties`、`schemaProperties`、`items`、`columns` 等结构抽取数据项。
- 持久化实现：新增 `cloudpivot_data_items` 作为新的数据项表；历史 `cloudpivot_entity_fields` 仅作为兼容表保留。新增 Flyway `V3__cloudpivot_data_items_and_relations.sql`，用于创建数据项表、迁移历史数据，并在 `cloudpivot_entity_relations` 中补充 `source_data_item_id`。
- 关系约束：实体关联关系只允许由可解析目标实体的“关联表单”数据项生成；关系必须通过 `source_data_item_id` 回查到来源数据项，无法解析来源数据项或目标实体时跳过关系，不凭空推断。
- 检索增强：`metadata_search_documents` 新增 `data_item` 和 `relation` 类型文档；当用户输入精确数据项编码，例如 `customerType`、`opportunityCustomer` 时，编码精确命中的数据项优先于泛化业务词命中。
- 文档同步：新增并更新 `docs/technical-design/details/07-cloudpivot-metadata-sync.md` 与 `docs/technical-design/details/04-data-model.md`，明确实体模型、数据项、关联表单关系、同步响应计数和验收标准。
- QA 验证：定向执行 `CpClawApiTests,MessageStorageTests` 通过，结果为 Tests run: 4, Failures: 0, Errors: 0, Skipped: 0；完整执行 `server/` 下 `mvn test` 通过，结果为 Tests run: 9, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 既有第三方 PURE 注释和 chunk 体积提示。
- 最新验证：已按用户要求重新触发 `/api/metadata/sync`，返回 `appCount=29`、`entityCount=940`、`dataItemCount=16868`、`relationCount=936`、`searchDocumentCount=18773`；当前可继续抽查商机、客户等实体的数据项和关联关系是否来自真实云枢配置。

### 连续多轮追问性能稳定化

- 状态：已完成本轮代码实现、自动化测试和真实服务双 QA 连续追问压测，待用户在前端继续验证。
- 用户反馈：同一会话多问几次后仍会卡住，尤其是“新客户多还是老客户多”“分别属于什么阶段”“再看一下列表”等连续追问。
- 根因定位：`AgentOrchestrator` 之前没有把“新客户/老客户/新老客户/哪个多/还是”等表达识别为分析维度和上下文追问；运行态服务对未命中的分析/列表路径仍可能拉取过多记录；`MvpCloudPivotConnector` 在 pageSize 小于 50 时会默认给列表记录补详情，普通列表 10 条也可能被详情接口拖慢；阶段/状态分布缺少列表字段时会进入重型详情补齐路径。
- 已处理：新增新老客户比较意图、分析维度和上下文继承词；新老客户聚合优先识别 `customerType`、`customer_type`、`客户类型`、`新老客户`、`是否新客户`、`客户属性`、`客户性质` 等字段，字段不存在时快速说明缺字段；普通列表默认只查 10 条且不补详情；省份、阶段、新老客户优先取 100 条轻量样本；阶段/状态列表字段不可见时最多补 2 条详情样本；非显式 `enrichAllDetails=true` 时连接器不再自动逐条拉详情。
- 自动化测试：`CpClawApiTests` 已覆盖“系统有多少个客户？ -> 这些客户都属于哪些省份？ -> 新客户多还是老客户多”三轮链路，断言继承对象为是、分析维度为新老客户、命中 `crm_customer`，且不跳到 `system_customer` 或 `Test009`。
- 测试结果：`server/` 下完整执行 `mvn test` 通过，结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0。
- 真实服务复测：第一轮“系统有多少个客户？”约 2.9 秒，第二轮“这些客户都属于哪些省份？”约 0.9 秒，第三轮“新客户多还是老客户多”约 0.8 秒，均继承 `crm_customer`；真实客户列表样本没有可识别省份/客户类型字段，因此系统快速返回缺字段说明。
- 双 QA 连续追问：QA-A 客户链路连续 5 轮全部返回，最慢约 2.6 秒；QA-B 商机链路连续 5 轮全部返回，最慢约 2.5 秒；普通列表追问约 0.5 秒；每轮 `Observe` 都能继承上一轮真实对象和 `schemaCode`。
- 当前服务：后端新版进程已启动在 `http://127.0.0.1:8080/`，前端 Vite 仍可通过 `http://127.0.0.1:5173/` 访问。

### 第二轮客户省份追问快路径优化

- 状态：已完成本轮代码实现和自动化回归，待用户在前端继续验证。
- 用户反馈：第一轮问“系统有多少个客户？”已返回客户总数后，第二轮追问“这些客户都属于哪些省份？”等待时间过长；该追问应基于上一轮客户对象继续执行，不应重新走重型对象理解路径。
- 根因定位：`AgentOrchestrator` 原先遇到追问中包含“客户”这类业务词时，会倾向于不继承上一轮对象，导致重新执行元数据召回；运行态服务没有省份/区域分布专用分支，明确统计追问可能继续进入通用分析或列表路径；批量查询在非全量详情模式下仍默认每页补少量详情，真实数据量较大时会放大网络耗时。
- 已处理：同一对象 + 追问/维度词时继承上一轮运行态对象和真实 `schemaCode`，继承成功时直接构造元数据命中结果并跳过重复召回；新增省份、地区、区域、城市、地址等维度识别与按省份聚合；非全量详情批量查询不再默认每页补详情；省份追问先限量探测 200 条样本，避免缺少省份字段时全量翻页。
- 测试结果：新增自动化用例覆盖“系统有多少个客户？ -> 这些客户都属于哪些省份？”；定向执行 `server/` 下 `CpClawApiTests` 通过；完整执行 `server/` 下 `mvn test` 通过，结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0；真实服务复测第一轮客户计数约 1.9 秒，第二轮省份追问约 0.66 秒，`Act` 显示 `schemaCode=crm_customer`、`returned=200`。
- 待用户验证：刷新当前 `http://127.0.0.1:5173/` 后，在同一会话连续输入客户数量与省份追问，观察第二轮是否快速返回“按省份分布”或缺省份字段说明，并在右侧流程中看到继承对象为“是”、`returned=200`。



### 元数据向量库真实初始化与双 QA 验证

- 状态：已完成本轮修复、初始化和双 QA 验证，待用户基于前端继续体验确认。
- 用户要求：先把元数据向量库初始化好，再启动 2 个测试专家做系统性验证，不再让用户充当第一轮测试者。
- 根因定位：初始同步能返回 `appCount=29`、`entityCount=936`、`searchDocumentCount=965`，但 pgvector 表未写入；日志显示 Embedding 服务返回空向量。进一步排查发现，一次性发送 965 条文本会失败，改为 32 条一批后仍有大量批次被 Embedding 服务以 400 拒绝，导致只写入 5 条。
- 已处理：后端 Embedding 客户端改为小批量请求，并在 400/413 时递归拆分批次重试；过滤空文本时保留原始元数据文档下标，避免向量和文档错位；新增单元测试覆盖批处理、失败拆小重试和原始下标保持。
- 已初始化：重新启动后端并触发 `/api/metadata/sync`，真实同步结果为 `appCount=29`、`entityCount=936`、`searchDocumentCount=965`；PostgreSQL pgvector `metadata_vector_documents` 已写入 965 条，`embedding_dimension` 最小/最大均为 1024，`object_type` 分布为 app=29、entity=936，`metadata_vector_schema.embedding_model=text-embedding-v4`。
- QA-A 后端与数据测试专家结论：通过。验证 MySQL 元数据总数 965、pgvector 总数 965、维度 1024、对象分布 app=29/entity=936；`int_bu_oppor` 与 `crm_customer` 仍保持编码精确优先；“帮我看看销售进展池子”可通过向量语义召回命中销售相关对象，reason 包含 `向量语义召回 similarity=...`。
- QA-B 业务与前端测试专家结论：通过。`http://127.0.0.1:5173/` 可访问；同一新会话连续询问“系统有多少商机？”、“都处于什么阶段？”、“系统有多少个客户？”均返回真实云枢业务结果，不包含 `system_opportunity` 或 `system_customer`；商机命中 `int_bu_oppor` total=237，阶段分布为未签约 135、暂停 13、已签约 65、停止 20、丢单 4；客户命中 `crm_customer`，本轮实时 total=8711；三个 `agentRunId` 均可通过 `/api/audit/agent-runs/{id}` 恢复 Observe、Think、Act、Reflect 和工具调用记录。
- 已验证：`server/` 下执行 `mvn test` 通过，结果为 Tests run: 8, Failures: 0, Errors: 0, Skipped: 0；后端当前运行在 `http://127.0.0.1:8080/`，前端 Vite 当前运行在 `http://127.0.0.1:5173/`。
- 风险提示：`GET http://127.0.0.1:8080/` 根路径当前返回 500，但业务 API 正常；如需要独立健康检查或根路径欢迎页，后续需单独补充。


### 右侧处理流程点击恢复

- 状态：已完成本轮修复并通过验证，待用户浏览器侧确认。
- 用户反馈：点击 CPClaw 系统输出后，右侧“后端处理流程”仍显示“还没有可查看的流程”。
- 根因判断：新发送消息时右侧流程只保存在前端内存中；刷新页面或打开历史会话后只恢复消息列表，没有从助手消息找到对应 Agent Run，也没有按需查询审计记录，所以点击历史 CPClaw 回复不会更新右侧面板。
- 已处理：后端在每条助手消息 `metadataJson` 中写入 `agentRunId`；运行态查询回复继续保留 `source=runtime-query`、`entityName`、真实 `schemaCode`、total、returned 和 sourceEndpoint，不破坏多轮对象继承。
- 已处理：前端点击 CPClaw 回复时先查内存 trace；没有内存 trace 时解析消息 metadata 并调用 `/api/audit/agent-runs/{id}`，用 `planJson`、`reflectionJson` 和工具调用记录恢复 Observe、Think、Act、Reflect；旧消息只有运行态 metadata 时展示有限摘要，完全没有可恢复 metadata 时给出中文提示。
- 已同步：更新 Agent 设计文档、MVP 测试用例和 SubAgent 协作看板，新增“点击历史回复恢复右侧处理流程”验收口径。
- 已验证：`web/` 下 `npm run build` 通过；`server/` 下使用 Maven 3.9.11 执行 `mvn test` 通过，结果为 Tests run: 6, Failures: 0, Errors: 0, Skipped: 0；后端测试新增断言覆盖助手消息 metadata 持久化 `agentRunId`。
### 团队测试轮

- 状态：已完成本轮
- QA 测试工程师牵头，项目经理跟踪风险，前端和后端工程师分别支持问题定位。
- 本轮已覆盖：前端构建、主要路由 HTTP 检查、后端 `mvn test`、临时 H2 后端启动、设置相关 API、元数据同步、元数据搜索、对话发送、写操作确认和审计查询。
- 本轮结论：代码级与临时后端联调通过；默认 MySQL 持久化环境因本地 `3306` 未监听仍未验证，不判定生产持久化链路完全通过。

### SubAgent 协作看板

- 状态：已完成
- 已新增 `SUBAGENTS.md`，作为仓库内稳定的 SubAgent / 协作角色看板。
- 已覆盖产品经理、项目经理、UI 设计师、前端工程师、后端工程师、QA 测试工程师六个角色。
- 已记录当前任务拆解、角色工作记录、阻塞风险、同步记录和维护规则。
- 后续每次任务前后可同步更新该文件，避免依赖 Codex 客户端右侧面板是否展示 SubAgent。

### 对话工作台

- 状态：本地已实现，待提交
- 已改造为对话优先的工作台结构。
- 已增加历史会话列表和会话切换。
- 已增加新建会话入口。
- 已增加模型选择和思考模式开关。
- 已增加附件上传入口，并在发送下一条消息时携带上传附件 ID。
- 已保留 Agent 执行流程、元数据匹配、风险等级、候选对象和执行步骤数据；普通对话流默认不展示内部流程，排查时通过审计入口查看。

### 分析类 Agent 意图修复

- 状态：已完成本轮纠偏并通过后端自动化验证和真实云枢环境复测
- 用户反馈：输入“分析系统中的商机信息”时，系统回复“我还没有理解你要操作的云枢对象”，没有按业务语义理解为查询商机数据并分析。
- 根因判断：后端意图识别词表未把“分析、洞察、诊断、趋势”等词归为读数据场景；后续又错误地用本地 fallback 演示模型补齐商机/客户，导致系统能返回看似完整但不真实的业务结果。
- 已处理：保留 `analyze_data` 意图和模型分析调用链路，但移除 fallback 中的“系统商机/系统客户”演示业务模型和演示运行态数据；只有命中真实云枢 Metadata Index 中的实体并成功调用真实运行态接口后，才生成分析结论。
- 已验证：后端自动化测试用 mock 真实云枢元数据验证 `int_bu_oppor` 商机模型可被命中并查询；真实云枢复测“分析系统中的商机信息”命中 `商机 / int_bu_oppor`，运行态 total=237、returned=237 后生成分析；另有 fallback guard 用例验证演示环境下不会返回 `system_opportunity`、商机样本或假总数。

### 对话式澄清能力

- 状态：已完成本轮修复并通过验证
- 用户反馈：当 CPClaw 对用户意图识别不准确时，应根据已掌握的信息继续追问，引导用户补充信息，而不是只说没有理解。
- 已处理：新增 `clarify_intent` 分支；未知意图、读数据但未匹配到可查询对象、写操作但对象不明确时，都会先发起澄清，不触发高风险确认，也不执行工具。
- 澄清内容：说明系统目前理解到的业务对象、是否找到候选云枢对象，并提示用户补充应用/表单、动作类型和筛选条件；若 Metadata Index 有对象，会列出部分可选对象。
- 已验证：发送“帮我处理一下”返回 `intent=clarify_intent`，无需二次确认，并提示补充“查询/分析哪个应用或表单、想做什么动作、有没有筛选条件”。

### 明确统计查询执行

- 状态：已完成本轮纠偏并通过后端自动化验证和真实云枢环境复测。
- 用户反馈：输入“系统有多少商机？”时，意图和对象已经明确，系统不应继续要求用户补充云枢对象，而应直接查询系统中的商机数量。
- 根因判断：意图识别已命中“多少”并归类为查询，但此前本地元数据业务词抽取不稳；随后加入的 fallback 演示“系统商机”又污染了真实业务结果，产生不存在的 `system_opportunity` 编码。
- 已处理：继续保留计数表达清洗和业务对象抽取；但业务查询必须命中真实云枢元数据中的模型编码，fallback 演示编码会被拦截；本轮新增业务词全量候选召回与统一排序，避免真实 CRM 商机模型被同名辅助表截断。
- 已验证：后端自动化测试用 mock 真实元数据验证“系统有多少商机？”命中 `商机 / int_bu_oppor` 并返回 mock 真实运行态 total=237；真实云枢复测同样命中 `int_bu_oppor`，运行态 total=237；fallback guard 验证没有真实业务元数据时不会返回 `system_opportunity` 或 `总计 **3** 条`。

### 客户计数与按年分析执行

- 状态：已完成本轮纠偏并通过后端自动化验证和真实云枢环境复测。
- 用户反馈：用户说“系统有多少个客户”时，应查客户数据并返回客户总数；用户说“每年的客户量情况怎么样”时，应拿到客户数据并按年分析，而不是要求用户补充云枢对象。
- 根因判断：现有意图识别和业务词清洗已能处理商机计数，但此前用客户演示元数据和演示运行态数据补齐能力，违背了用户要求的真实云枢数据链路。
- 已处理：移除 fallback 客户演示模型和演示运行态数据；业务词清洗继续剥离“每年、按年、年度、年份、情况怎么样、量”等噪声词，保留“客户”参与真实元数据匹配。
- 已处理：按年分析类请求会识别为 `analyze_data`，运行态查询 pageSize 提升到 200，并在真实云枢接口下分页拉取完整数据；当前保护上限为 20,000 条，避免只拿第一页样本就输出全量结论，同时只对少量样本补详情以控制性能。
- 已处理：按年聚合回答会根据匹配对象生成“按年客户量分析”等对象化文案，返回年度分布、年份覆盖、峰值年份、趋势判断和下一步下钻建议。
- 已验证：`server/` 下 `mvn test` 通过，mock 真实元数据覆盖“系统有多少个客户？”命中 `客户 / crm_customer`、总计 `58` 条；真实云枢复测“系统有多少个客户？”命中 `crm_customer`，运行态 total=8694；“每年的客户量情况怎么样？”命中 `crm_customer`，total=8694、returned=8694 后按年聚合；fallback 环境不再返回 `system_customer` 或假客户总数。

### 执行过程可见化与对象串线排查

- 状态：已完成本轮增强并通过后端自动化验证，待真实云枢环境复测。
- 用户反馈：页面里不管问商机还是客户都返回 3，且希望看到 Agent 如何理解用户意图、如何转成查找动作、拿到的原始数据是什么、最后如何总结输出。
- 复现结论：旧版本的根因不是前端展示，而是后端执行链允许 fallback 演示模型进入业务问答，导致系统看似查询了云枢，实际拿到的是本地演示编码和演示数据。
- 已处理：Agent 步骤和审计记录保留动作、业务对象、分析维度、筛选、候选对象、候选编码、total/returned、原始摘要和 Reflection 结果；读数据类回答正文按最新反馈不再展示 `### 执行过程`。
- 已验证：`server/` 下 `mvn test` 通过，并新增断言：真实样例下商机命中 `schemaCode=int_bu_oppor` 且客户命中 `schemaCode=crm_customer`；fallback 环境下“系统有多少商机？”不会返回 `system_opportunity`、演示样本或假总数。

### 向量语义检索增强

- 状态：已完成本轮后端实现、技术文档同步和自动化测试，待真实环境启用验证。
- 用户输入：向量数据库 PostgreSQL/pgvector 已准备，Embedding 模型使用阿里云百炼 OpenAI-compatible `text-embedding-v4`；要求先写入技术方案，再开始实现向量检索，提升用户语义理解准确性。
- 已处理：技术方案已写入 `docs/technical-design/details/06-key-technical-strategy.md`，并同步更新技术蓝图、Agent 设计、云枢集成、数据模型、产品需求、测试计划和项目看板；文档只保留环境变量占位符，未写入明文密码或 API Key。
- 已处理：后端新增 `com.cpclaw.vector` 包，包含 Embedding 客户端、向量配置、pgvector 元数据向量索引、向量候选模型和 `MetadataVectorSearch` 接口。
- 已处理：`MetadataService.initializeCloudPivotMetadata()` 在写入 MySQL Metadata Index 后容错写入 pgvector；向量写入失败只记录降级信息，不影响元数据初始化。
- 已处理：`MetadataSearchService` 在确定性召回基础上合并向量候选；向量候选必须通过 `document_id` 回到 MySQL `metadata_search_documents`，只能加有限分，不能覆盖编码精确、名称精确、显式路径和多轮上下文继承。
- 已处理：新增 PostgreSQL JDBC 依赖和 `CPCLAW_VECTOR_*`、`CPCLAW_EMBEDDING_*` 配置项；默认 `CPCLAW_VECTOR_ENABLED=false`，不影响现有环境。
- 已验证：`server/` 下执行 `mvn test` 通过，结果为 Tests run: 4, Failures: 0, Errors: 0, Skipped: 0；新增测试覆盖向量语义召回“帮我看看销售进展池子”命中真实商机，以及向量候选不能覆盖直接 `int_bu_oppor` 编码精确匹配。
- 已验证：执行 `git diff --check` 通过，仅有 Windows 换行提示；敏感信息扫描未发现明文 API Key 或数据库密码进入代码、文档和进度文件。
- 待验证：真实 pgvector 表创建、Embedding 服务调用、元数据初始化时向量写入、真实自然语言语义召回效果仍需在启用环境变量后联调。

### 对话回答展示与 Markdown 渲染

- 状态：已完成本轮代码调整，已通过自动化验证和本地接口冒烟，待用户验证。
- 用户反馈：普通对话不需要展示执行过程，同时助手回复需要正确处理 Markdown 格式。
- 已处理：后端读数据类回答正文不再拼接 `### 执行过程`，只返回业务结论、分析和建议；真实 `schemaCode`、运行态来源、total/returned、原始数据摘要继续保留在 Agent 步骤、Agent Run、Reflection 和工具调用审计中。
- 已处理：运行态查询失败时，助手正文改为面向用户的错误说明，不再展示意图理解、对象匹配、反思检查等内部过程。
- 已处理：前端 `MarkdownMessage` 改为使用 Markdown 解析和 HTML 安全净化，支持标题、列表、加粗、代码和表格；历史消息若已包含旧版 `### 执行过程` 前置块，展示层会自动隐藏执行过程，只保留业务结论。
- 已处理：聊天主界面默认不再展示“后端处理流程”卡片，避免普通用户看到内部执行链；排查时通过审计页和工具调用记录查看。
- 已同步：更新 Agent 设计、关键技术策略和测试用例文档，统一“默认对话展示业务结果，审计记录保留执行过程”的口径。
- 已验证：后端 `mvn test` 通过，结果为 4 个测试全部通过、0 失败、0 错误、0 跳过。
- 已验证：前端 `npm run build` 通过；仅保留既有 Rollup PURE 注释和 chunk 体积提示。
- 已验证：新后端已用 MySQL `CPClaw` 持久化库启动，前端 `http://127.0.0.1:5173/` 和后端 `http://127.0.0.1:8080/` 均可访问。
- 已验证：通过 `5173` 前端代理发送“系统有多少商机？”返回 `query_data`，助手正文为“已在云枢中查询到‘商机’对应数据，总计 **237** 条。”，未出现 `### 执行过程`。

### ReAct + Reflection MVP 执行链

- 状态：已完成本轮实现并通过后端自动化验证。
- 用户反馈：当前仍是 MVP 规则型 Agent，不是完整 ReAct + Reflection，需要改成 ReAct + Reflection 模式。
- 已处理：后端 `AgentOrchestrator` 已拆分为 `Observe -> Think -> Plan -> Act -> Reflect`；`Observe` 汇总用户目标和最近会话上下文，`Think` 识别意图、匹配元数据并抽取缺失槽位，`Plan` 生成安全动作列表，`Act` 执行元数据检索、云枢查询、澄清或确认，`Reflect` 检查是否完成、是否需要用户补充、是否等待确认。
- 已处理：响应执行步骤固定保留 `Observe 观察上下文`、`Think 理解意图`、`Act 执行动作`、`Reflect 反思检查`，用于审计和开发排查；普通对话流默认不展示执行过程。
- 已处理：审计 `plan_json` 写入 `mode=react-reflection-mvp`、观察结果、思考结果和计划动作；`reflection_json` 写入反思状态、检查项、是否通过和是否需要用户输入。
- 已处理：多轮对象级上下文继承已落地。真实运行态查询成功后，助手消息 metadata 保存最近命中的业务对象和真实 `schemaCode`；下一轮“分别在什么阶段？”“按负责人看一下”“金额分布呢？”这类承接式追问会生成包含上一轮 `schemaCode` 的 `effectiveQuery`，重新走元数据匹配和运行态查询。
- 已处理：同名对象上下文已加固。上一轮如果命中“项目基础数据 / 商机 / business_opportunity”，追问“分别在什么阶段？”会继续继承 `business_opportunity`，不会跳回默认 CRM 商机 `int_bu_oppor`。
- 已处理：阶段/状态类追问会重新查询真实云枢运行态数据，不复用上一轮总数结论；运行态详情补齐已增加受控并发和记录上限，避免超大对象无限串行请求。
- 已处理：多轮记录级引用仍按安全澄清处理，例如“给第一条商机写一条跟进记录”因缺少“跟进内容”不会直接创建写操作确认。
- 已验证：`server/` 下 `mvn test` 已补充 ReAct 步骤、审计 `reflectionJson`、写操作 `pending_confirmation` 和跟进内容缺失澄清断言。
- 已验证：新增定向回归覆盖“商机数量后追问阶段分布”和“显式应用路径同名商机追问不串线”；`CpClawApiTests` 单测通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 已验证：真实 MySQL 后端经 `5173` 前端代理连续发送“系统有多少商机？”与“分别在什么阶段？”，第二轮 `Observe` 有效目标为 `int_bu_oppor 商机 分别在什么阶段？`，返回阶段分布未签约 135、暂停 13、已签约 65、停止 20、丢单 4。
- 未完成边界：当前仍不是生产级完整智能体，尚未完成模型驱动结构化意图、DAG 多节点编排、多工具循环执行、记录级上下文 ID 绑定和真实云枢业务环境的完整回归。
- 性能边界：真实阶段分布目前仍依赖运行态详情补齐，准确性已通过，但接口耗时偏长；后续需通过字段级元数据、列表显示字段配置或云枢聚合接口继续优化响应速度。

### 智能体智能程度专项测试

- 状态：已完成本轮，待用户继续体验验证。
- QA 测试专家通过 `http://127.0.0.1:5173/` 前端代理链路执行 14 条自然语言用例，覆盖明确查询、统计变体、客户计数、按年客户量分析、分析意图、模糊澄清、未知对象、高风险写操作确认和复杂上下文探索。
- 严格验收结果：13 条严格用例全部通过，严格通过率 100%。
- ReAct + Reflection 回归结果：1 条复杂上下文用例“给第一条商机写一条跟进记录”进入 `clarify_intent`，候选对象为“系统商机”，缺失槽位为“跟进内容”，符合当前安全预期；完整上下文记录引用仍需下一阶段增强。
- 测试报告：`docs/test-cases/11-agent-intelligence-validation-2026-06-21.md`。

### 设计文档同步规则

- 状态：已完成本轮更新
- 用户要求：所有设计改动都要同步更新至文档；系统期望是足够智能，能够理解用户意图并执行任务。
- 已处理：更新 `docs/README.md`，明确设计变更同步规则和文档责任边界。
- 已处理：更新 `docs/product-design/details/01-requirements.md` 和 `docs/product-design/details/02-product-plan.md`，固化“理解意图、必要时澄清、执行任务、解释结果”的产品基线。
- 已处理：更新 `docs/technical-design/details/02-agent-design.md`，补充 `analyze_data`、`clarify_intent`、澄清策略和分析类任务链路。
- 已处理：更新 `docs/test-cases/08-mvp-test-cases.md`，新增分析类自然语言意图和模糊意图澄清验收用例。
- 已处理：本轮 ReAct + Reflection MVP 改造后，同步更新 `docs/CLAUDE.md` 的后端架构说明，并在 `docs/test-cases/08-mvp-test-cases.md` 新增 ReAct + Reflection 执行链与审计验收用例。

### 设置页

- 状态：本地已实现，待提交
- 已扩展为普通用户云枢账号、管理员元数据环境、模型配置三个区域。
- 已增加凭据已保存状态提示。
- 已增加普通用户和管理员连接测试入口。
- 已增加模型列表和模型能力展示。
- 已修复保存接口在后端未启动时的前端提示：网络失败或无业务消息的 `5xx` 会显示“后端服务不可用，请确认后端已启动后重试。”，后端返回的结构化业务错误仍优先展示。

### 账号与模型保存报错排查

- 状态：已修复并通过临时后端联调，MySQL 持久化待验证
- 复现现象：通过前端开发服务调用 `POST /api/settings/user` 时返回 `500` 且响应体为空。
- 根因判断：`127.0.0.1:8080` 当前无后端服务监听，Vite 代理无法转发到 Spring Boot 后端。
- 已处理：更新前端请求封装，让保存账号、保存模型、连接测试等接口在后端不可用时给出明确中文提示。
- 后续联调发现：在干净数据库上同时触发 `GET /api/settings` 和 `POST /api/settings/user` 时，后端读取接口会创建默认设置行，可能与保存接口并发抢占 `system_settings.default` 主键并返回 `500`。
- 已处理：后端设置读取与连接测试改为只返回内存默认值，不再在读路径落库；只有保存操作会创建默认设置行，并新增自动化测试覆盖该行为。
- 已验证：使用 H2 临时后端启动 `8080` 后，通过 `5173` 前端代理并发调用 `GET /api/settings` 与 `POST /api/settings/user`，两者均返回 200。
- 待验证：启动真实 MySQL 数据库后，重新执行账号保存、模型新增、管理员环境保存和连接测试的持久化验证。

### 元数据页

- 状态：本地已实现，待提交
- 已恢复元数据路由和导航入口。
- 已增加元数据同步按钮、搜索、加载状态、错误状态和同步结果摘要。

### 审计页

- 状态：本地已实现，待提交
- 已恢复审计路由和导航入口。
- 已增加 Agent Run 查询、空态、未找到态、加载态和脱敏工具调用表格。

## 验证记录

- 2026-06-20：`web/` 下执行 `npm run build`，结果通过。
- 2026-06-20：执行 `git diff --check`，结果通过；仅出现 Windows 换行提示。
- 2026-06-20：前端开发服务本地运行于 `http://127.0.0.1:5173/`。
- 2026-06-20：前端路由 HTTP 检查通过：`/`、`/settings`、`/metadata`、`/audit` 均返回 200。
- 2026-06-20：敏感信息 diff 检查未发现原始云枢账号密码、明显 Bearer Token 或 API Key 字面量。
- 2026-06-20：尝试后端 `mvn test`，失败原因是 Maven Central 依赖下载过程中网络中断，报错包含 `Connection reset` / `Network is unreachable`。这不是测试断言失败。
- 2026-06-20：复现账号与模型页面保存错误：`POST http://127.0.0.1:5173/api/settings/user` 返回 `500` 且响应体为空；`127.0.0.1:8080` 端口检查结果为未监听。
- 2026-06-20：修复前端通用请求封装，后端不可用或空 `5xx` 响应会显示明确中文提示，同时保留后端结构化错误消息的展示优先级。
- 2026-06-20：修复后重新执行 `web/` 下 `npm run build`，结果通过；`/settings` 路由 HTTP 检查返回 200。
- 2026-06-20：再次尝试后端 `mvn test`，依赖下载已能继续，但受 Playwright/Maven 大依赖下载耗时影响，未在本轮完成完整后端测试结论。
- 2026-06-20：新增 `SUBAGENTS.md` 协作看板，记录六个 SubAgent 角色、当前任务、阻塞风险和维护规则。
- 2026-06-20：启动团队测试轮，计划覆盖前端构建、主要路由、开发服务、后端端口、后端自动化测试和关键 API 可用性。
- 2026-06-20：本轮团队测试重新执行 `web/` 下 `npm run build`，结果通过；Vite/Rollup 仅提示大 chunk 与第三方库 PURE 注释警告。
- 2026-06-20：本轮团队测试执行前端路由 HTTP 检查：`/`、`/settings`、`/metadata`、`/audit` 均返回 200。
- 2026-06-20：本轮团队测试执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-20：检查本地默认后端依赖端口，`127.0.0.1:3306` 未监听，因此默认 MySQL 持久化环境未完成验证。
- 2026-06-20：使用 H2 内存库临时启动后端 `8080`，经 `http://127.0.0.1:5173` 前端代理验证 `GET /api/settings` 返回 200。
- 2026-06-20：在临时后端联调中发现首次 `GET /api/settings` 与 `POST /api/settings/user` 并发时可能触发 `system_settings.default` 唯一键冲突，导致保存接口返回 500。
- 2026-06-20：修复后端设置读路径副作用：读取设置和连接测试不再创建默认设置行，保存时才创建；新增 `CpClawSettingsTests` 覆盖“读取不落库、保存才落库”。
- 2026-06-20：修复后在干净 H2 临时后端上并发复测 `GET /api/settings` 与 `POST /api/settings/user`，两者均返回 200，账号与模型保存链路经 `5173` 代理通过。
- 2026-06-20：顺序执行核心冒烟链通过：管理员环境保存、普通账号连接测试、管理员连接测试、元数据同步、元数据搜索、创建会话、查询消息、修改类消息二次确认、确认提交、审计查询均返回 200。
- 2026-06-21：修复“分析系统中的商机信息”意图理解问题，新增分析类意图、业务关键词匹配、商机演示元数据、模型分析调用入口和规则分析兜底。
- 2026-06-21：执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；其中包含“分析系统中的商机信息”回归用例。
- 2026-06-21：执行 `web/` 下 `npm run build`，结果通过；仅保留 Vite/Rollup 既有 chunk 体积和第三方注释警告。
- 2026-06-21：重启 H2 临时后端后，经 `5173` 代理验证“分析系统中的商机信息”：设置保存 200、元数据同步实体数 6、对话返回 `analyze_data`、候选对象“系统商机”、无需二次确认，并返回分析型答案。
- 2026-06-21：新增对话式澄清能力，模糊输入“帮我处理一下”返回 `clarify_intent`，提示用户补充应用/表单、动作类型和筛选条件。
- 2026-06-21：再次执行 `server/` 下 `mvn test` 与 `web/` 下 `npm run build`，结果均通过；后端回归包含澄清意图用例。
- 2026-06-21：重启 H2 临时后端后，经 `5173` 代理验证“帮我处理一下”：元数据同步实体数 6、对话返回 `clarify_intent`、无需二次确认，并展示澄清问题和当前可选对象。
- 2026-06-21：根据用户要求同步更新设计文档：`docs/README.md`、产品需求、产品规划、Agent 详细设计和 MVP 测试用例均已补充“文档同步规则”和“智能理解-澄清-执行-解释”基线。
- 2026-06-21：修复“系统有多少商机？”明确统计查询误进入澄清的问题，增强业务词清洗、计数意图识别和运行态计数回答，并补充自动化回归用例。
- 2026-06-21：执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；其中包含“系统有多少商机？”回归用例。
- 2026-06-21：执行 `web/` 下 `npm run build`，结果通过；仅保留 Vite/Rollup 既有 chunk 体积和第三方注释警告。
- 2026-06-21：重启 H2 临时后端后，经 `5173` 代理验证“系统有多少商机？”：元数据同步实体数 6、对话返回 `query_data`、候选对象“系统商机”、无需二次确认，并直接返回总计 `3` 条。
- 2026-06-21：QA 测试专家完成智能体智能程度专项验证：12 条自然语言用例中 11 条严格验收全部通过，1 条复杂上下文探索用例记录为后续能力缺口；新增专项报告 `docs/test-cases/11-agent-intelligence-validation-2026-06-21.md`。
- 2026-06-21：专项测试报告补充后再次执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-21：落地 ReAct + Reflection MVP 执行链：后端每轮对话经过 Observe、Think、Plan、Act、Reflect，响应步骤展示四阶段，审计写入 `plan_json` 与 `reflection_json`；自动化测试补充阶段步骤、审计反思和安全澄清断言。
- 2026-06-21：本轮最终验证执行 `git diff --check`，结果通过；仅出现 Windows 换行提示。
- 2026-06-21：本轮最终验证执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；覆盖 ReAct 步骤、审计反思、写操作确认和跟进内容缺失澄清断言。
- 2026-06-21：本轮最终验证执行 `web/` 下 `npm run build`，结果通过；仅保留 Vite/Rollup 既有 chunk 体积和第三方 PURE 注释警告。
- 2026-06-21：同步维护文档和 MVP 测试用例：`docs/CLAUDE.md` 更新 Agent 架构说明，`docs/test-cases/08-mvp-test-cases.md` 新增 ReAct + Reflection 执行链与审计用例。
- 2026-06-21：启动最新 H2 临时后端用于用户验证，后端监听 `http://127.0.0.1:8080/`，前端 Vite 继续监听 `http://127.0.0.1:5173/`。
- 2026-06-21：通过最新后端完成冒烟验证：设置保存、管理员环境保存、元数据同步实体数 6、发送“系统有多少商机？”返回 `query_data`、候选对象“系统商机”、执行步骤 Observe/Think/Act/Reflect、答案包含“总计 **3** 条”；审计 `reflection_json` 包含 `react-reflection-mvp`；同会话发送“给第一条商机写一条跟进记录”返回 `clarify_intent`、候选对象“系统商机”、缺失“跟进内容”、步骤包含 `Reflect 反思检查`。
- 2026-06-21：修复客户类自然语言意图执行：新增“系统客户”fallback 元数据和 4 条客户演示运行态数据，增强“系统有多少个客户？”计数查询与“每年的客户量情况怎么样？”按年分析能力，当前 fallback 元数据同步实体数为 7。
- 2026-06-21：增强云枢运行态分析类查询：计数类仍轻量取数，分析/按年类请求提高 pageSize，并在真实云枢接口下尽量分页拉取完整数据，初始设置 1000 条保护上限。
- 2026-06-21：本轮客户意图增强验证执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；覆盖客户总数、按年客户量分析、ReAct 步骤和既有商机/澄清/确认回归。
- 2026-06-21：同步维护文档和测试用例：`docs/technical-design/details/02-agent-design.md` 补充动作+对象+维度识别和按年客户量分析设计，`docs/test-cases/08-mvp-test-cases.md` 新增客户数量与按年客户量分析用例，`docs/test-cases/11-agent-intelligence-validation-2026-06-21.md` 更新为 14 条用例、fallback 实体数 7。
- 2026-06-21：本轮最终验证执行 `git diff --check`，结果通过；仅出现 Windows 换行提示。
- 2026-06-21：本轮最终验证执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-21：本轮最终验证执行 `web/` 下 `npm run build`，结果通过；仅保留 Vite/Rollup 既有 chunk 体积和第三方 PURE 注释警告。
- 2026-06-21：重启最新 H2 临时后端 `http://127.0.0.1:8080/` 与前端 Vite `http://127.0.0.1:5173/`，经 `5173` 代理验证：元数据同步实体数 7，“系统有多少个客户？”返回 `query_data`、候选对象“系统客户”，“每年的客户量情况怎么样？”返回 `analyze_data`、候选对象“系统客户”，答案包含趋势判断。
- 2026-06-21：根据用户反馈“现在访问不了了”复查本地服务：前端 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/settings` 返回 200，Vite 入口资源 `/@vite/client` 与 `/src/main.ts` 均返回 200；当前服务实际可访问。
- 2026-06-21：通过 `5173` 前端代理重新初始化 H2 临时环境设置和元数据，同步实体数为 7；复测“系统有多少个客户？”返回 `query_data`、候选对象“系统客户”、答案包含“总计 **4** 条”；复测“每年的客户量情况怎么样？”返回 `analyze_data`、候选对象“系统客户”、答案包含“按年客户量分析”、“2023 年：2 个客户”和“趋势判断”。
- 2026-06-21：根据用户反馈“商机和客户都返回 3 且需要展示思考过程”完成排查与增强：接口层复测商机为 3、客户为 4；读数据回答新增可审计执行过程，展示意图理解、对象匹配、schemaCode、原始数据摘要和结论生成；执行步骤状态新增中文摘要。
- 2026-06-21：本轮执行过程可见化验证执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；新增断言覆盖商机/客户 schema 与总数不串线、原始数据摘要可见。
- 2026-06-21：重启最新 H2 临时后端并经 `5173` 前端代理复测：元数据同步实体数 7；“系统有多少商机？”返回候选“系统商机”、`schemaCode=system_opportunity`、total=3、原始摘要包含“华东制造业数字化项目”；“系统有多少个客户？”返回候选“系统客户”、`schemaCode=system_customer`、total=4、原始摘要包含“华东制造集团”；“每年的客户量情况怎么样？”返回客户 schema、total=4、returned=4、按年客户量分析和趋势判断。
- 2026-06-21：根据用户反馈“`system_opportunity` 不应被当作真实 schemaCode”修正执行过程口径：`sourceEndpoint=local-fallback` 时显示“演示编码”，并明确说明不是真实云枢 schemaCode；只有真实云枢元数据/运行态查询才显示真实 `schemaCode`。
- 2026-06-21：执行 `server/` 下 `mvn test`，结果通过：Tests run: 3, Failures: 0, Errors: 0, Skipped: 0；覆盖 fallback 演示编码说明和非真实 schemaCode 提示。
- 2026-06-21：重启最新 H2 临时后端并经 `5173` 代理复测：商机回答显示“命中本地演示对象”“演示编码=`system_opportunity`”，并提示“不是真实云枢 schemaCode”；客户回答显示“演示编码=`system_customer`”，同样提示非真实云枢编码；前端执行步骤 `Act` 也显示“演示编码”而不是 `schemaCode`。
- 2026-06-21：根据用户进一步反馈“云枢业务问答必须查询真实元数据和真实运行态数据，不能返回不存在的 schemaCode”，完成关键纠偏：移除 fallback 商机/客户演示模型和运行态数据；运行态来源为 `local-fallback` 时拒绝生成业务答案；Agent `Think` 展示动作、业务对象、维度、筛选和候选编码；自动化测试改为 mock 真实云枢编码 `int_bu_oppor`、`crm_customer`，并新增 fallback guard 防止 `system_opportunity` / `system_customer` 再次进入业务答案。
- 2026-06-21：纠偏后执行 `server/` 下 `mvn test`，结果通过：Tests run: 4, Failures: 0, Errors: 0, Skipped: 0；覆盖真实 schema 执行链和 fallback 业务假数据保护。
- 2026-06-22：修复真实云枢同名对象排序问题：元数据检索不再只使用数据库前 10 条直接结果，而是用业务词全量召回候选后统一打分排序；真实复测“商机”第一候选为 `zlcsstcrm / 商机 / int_bu_oppor`，“客户”第一候选为 `zlcsstcrm / 客户 / crm_customer`，不再选到 `business_opportunity` 或 `Test009`。
- 2026-06-22：完成真实云枢环境验收：连接测试通过，元数据同步成功 `appCount=29`、`entityCount=922`、`searchDocumentCount=951`；“系统有多少商机？”返回 `schemaCode=int_bu_oppor`、total=237；“系统有多少个客户？”返回 `schemaCode=crm_customer`、total=8694；“分析系统中的商机信息”返回 `schemaCode=int_bu_oppor`、total=237、returned=237。
- 2026-06-22：根据用户“每年的客户量要拿全量客户信息再分析”的要求，运行态按年分析分页上限提升为 20,000 条，pageSize=200，并避免对所有记录逐条补详情；真实复测“每年的客户量情况怎么样？”返回 `schemaCode=crm_customer`、total=8694、returned=8694，已基于全量客户数据进行年度聚合。
- 2026-06-29：根据用户确认的关键技术策略新增 `docs/technical-design/details/06-key-technical-strategy.md`，明确配置和元数据必须持久化、云枢元数据是 Agent 知识底座、查询类直接执行、操作类确认后执行、不能准确匹配时通过对话澄清、交互需支持思考过程和流式输出。
- 2026-06-29：按用户要求“先不做向量化语义检索”，完成非向量元数据检索增强：精确名称、别名、编码、应用路径、全文关键词、复合业务词召回和辅助对象降权；新增 API 集成测试覆盖“销售机会=商机”、直接 `schemaCode`、客户下面的商机、CRM 客户等场景；`server/` 下 `mvn test` 通过，结果为 4 个测试全部通过。
- 2026-06-29：继续加固非向量检索的应用路径优先级：通用“商机/客户”问法仍优先真实 CRM 核心对象；当用户显式说出应用路径，例如“项目基础数据里的商机”时，检索排序尊重 `graphPath` 线索并命中该应用下的同名对象。同步更新技术蓝图、Agent 设计、云枢集成、数据模型、产品需求、测试计划和项目看板，统一当前阶段“不做向量化语义检索”的口径；`server/` 下 `mvn test` 通过，结果为 Tests run: 4, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-29：按用户要求把刚才讨论的技术方案继续存入技术文档：在 `docs/technical-design/details/06-key-technical-strategy.md` 新增“意图识别后的元数据匹配关键技术方案”，明确结构化意图输入输出、多路检索请求构造、候选召回、规则打分、图谱校验、字段/Action 补全、置信度阈值、澄清策略、执行与审计要求。
- 2026-06-29：根据用户反馈“同一会话中前面问商机多少、后面问分别在什么阶段，应承接商机上下文”，完成对象级多轮上下文继承增强：运行态查询成功后保存助手消息 metadata；追问时 `Observe` 恢复上一轮 `entityName` 和真实 `schemaCode`，生成 `effectiveQuery` 参与元数据匹配和运行态查询；新增显式应用路径同名商机回归，验证上一轮命中 `business_opportunity` 后追问阶段不会跳回 `int_bu_oppor`；定向执行 `CpClawApiTests` 通过。
- 2026-06-29：完成本轮多轮上下文真实代理复测：MySQL `CPClaw` 后端启动于 `8080`，前端代理 `5173` 可访问；同一会话先问“系统有多少商机？”返回 total=237，再追问“分别在什么阶段？”时 `Observe` 继承对象为是，有效目标包含 `int_bu_oppor 商机`，回答返回未签约 135、暂停 13、已签约 65、停止 20、丢单 4。
- 2026-06-29：针对阶段/状态分布补详情耗时问题，后端运行态详情补齐改为受控并发并设置记录上限；`server/` 下再次执行 `mvn test` 通过，结果为 Tests run: 4, Failures: 0, Errors: 0, Skipped: 0。真实接口阶段分布准确性已通过，但响应耗时仍偏长，后续需继续优化字段级元数据、列表字段或云枢聚合接口。
- 2026-06-29：根据用户新的布局调整要求，已在 `design/cpclaw-figma/figma-plugin/code.js` 新增 Figma 原型页“05 布局调整提案”，展示顶部导航替代左侧全局菜单、左侧仅保留历史会话、中央扩大对话主窗口、右侧展示并关联每轮助手回复的后端处理流程。当前仅完成原型，等待用户确认后再执行前端代码改造；已执行 `node --check design/cpclaw-figma/figma-plugin/code.js` 通过。
- 2026-06-29：根据用户反馈“前面更新的 UI 交互界面没有看到”，确认此前只生成了 Figma 原型，未落到运行中的前端页面；本轮已将布局调整实现到 `web/src/layouts/MainLayout.vue` 和 `web/src/views/ChatView.vue`：全局菜单移至顶部导航，对话页改为左侧历史会话、中间对话主窗口、右侧关联当前回复的后端处理流程；`web/` 下 `npm run build` 通过。
- 2026-06-29：根据用户提供的向量数据库和 Embedding 模型配置，完成混合元数据检索增强：文档新增 PostgreSQL pgvector + OpenAI-compatible Embedding 设计方案；后端新增向量配置、Embedding 客户端、pgvector 元数据索引和向量候选融合排序；元数据同步和检索均支持向量不可用时降级到确定性检索；新增测试覆盖语义召回和编码精确优先；`server/` 下 `mvn test` 通过，结果为 Tests run: 4, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-29：根据用户反馈“第二个问题抛出 Data too long for column 'content'”，完成缺陷定位与修复：根因为第二轮助手回答可能超过 MySQL `TEXT` 64KB 上限，导致 `messages.content` 入库失败；新增 Flyway `V2__expand_message_and_audit_text_columns.sql`，将 `messages.content`、`messages.metadata_json`、`agent_runs.plan_json`、`agent_runs.reflection_json`、`tool_calls.*_json_masked` 和确认单长文本字段扩为 `LONGTEXT`，并同步 JPA 实体注解。
- 2026-06-29：QA 测试工程师补充系统性回归：新增 `MessageStorageTests` 验证超过 64KB 的助手正文和 metadata 可完整入库，并检查迁移脚本覆盖消息与审计长文本字段；`server/` 下执行 `mvn test` 通过，结果为 Tests run: 6, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-29：完成真实 MySQL 迁移和复测：重新打包并启动后端，Flyway 日志显示 schema `cpclaw` 从版本 1 迁移到版本 2，之后服务启动于 `http://127.0.0.1:8080/`；经真实接口连续发送“系统有多少商机？”和“都处于什么阶段？”，第二轮不再出现 `content` 截断异常。
- 2026-06-29：QA 继续发现并修复口语化阶段追问误走明细列表的问题：将“处于/什么阶段/什么状态”纳入阶段分布识别，并把自动化用例从“分别在什么阶段？”调整为更贴近用户截图的“都处于什么阶段？”；重新执行 `server/` 下 `mvn test` 通过，结果为 Tests run: 6, Failures: 0, Errors: 0, Skipped: 0。
- 2026-06-29：完成真实云枢回归验证：同一会话先问“系统有多少商机？”返回 total=237，再追问“都处于什么阶段？”时 `Observe` 继承 `int_bu_oppor 商机`，第二轮返回“按阶段分布”：未签约 135、暂停 13、已签约 65、停止 20、丢单 4；助手正文长度约 215，不再返回 237 条明细列表，也不再触发数据库长文本错误。
- 2026-06-29：根据用户截图反馈“第二个问题输入很长时间没有响应，也没有思考输出”，完成前端等待态修复：接口复现显示“商机在什么阶段？”真实云枢查询最终成功但耗时约 69 秒；`ChatView` 已在发送后立即插入不落库的 CPClaw 处理中占位回复，并在右侧处理流程显示临时 Observe、Think、Act、Reflect，后端返回后替换为真实回答和真实步骤；`web/` 下 `npm run build` 通过。

- 2026-06-30：根据用户反馈“点击系统输出，没有在右侧展示思考过程”，修复右侧处理流程恢复链路：助手消息 metadata 持久化 `agentRunId`，历史会话点击 CPClaw 回复会懒加载 `/api/audit/agent-runs/{id}` 并重建 Observe、Think、Act、Reflect；旧运行态 metadata 可展示有限摘要；`web/` 下 `npm run build` 通过，`server/` 下 `mvn test` 通过，结果为 Tests run: 6, Failures: 0, Errors: 0, Skipped: 0。

## 当前阻塞与风险

- 默认生产配置依赖 MySQL；当前本机 MySQL 已可连接并用于启动后端，但账号保存、模型新增、管理员环境保存等持久化写入仍需单独回归。
- `SUBAGENTS.md` 需要在后续每个任务前后继续维护，否则看板会与真实执行状态脱节。
- 所有后续设计、交互、Agent 行为、接口、数据模型和验收标准改动都必须同步文档，否则实现与设计基线会脱节。
- 当前前端工作台改动还没有提交为功能提交。
- 当前已落地 ReAct + Reflection MVP 执行链和对象级多轮上下文继承，但生产级模型驱动结构化意图、DAG 多节点计划、多工具循环、记录级上下文 ID 引用和 Reflection 自修正仍需后续实现；澄清能力目前仍偏单轮，尚未把用户后续补充与上一轮澄清上下文自动合并。
- 阶段/状态分布当前已改为轻量列表优先和最多 2 条详情样本兜底，连续追问不再因全量详情补齐卡住；但当列表字段缺失时，阶段分布可能是样本级判断，后续仍应优先同步字段级元数据、列表显示字段或调用云枢聚合接口提升准确性。
- 向量检索真实服务联调已完成，当前通过环境变量启用；后续仍需把向量配置持久化、后台重建、进度展示和失败批次可观测性产品化，避免长期依赖手工环境变量和元数据同步链路。
- 当前向量索引重建仍是在元数据同步流程中容错执行，尚未改为独立后台任务；大量元数据场景下后续应加入重建入口、进度展示、失败记录和重试策略。
- 后端业务 API 当前可用，但根路径 `GET /` 返回 500；如需要运维健康检查或浏览器直接访问后端根路径，需补充 `/actuator/health` 或根路径处理。
- 附件目前只接入上传入口，尚未实现解析、字段映射和填单确认链路。
- 元数据初始化仍是 MVP 能力，字段、表单、流程、Action 等完整设计态采集仍需增强。
- 尚未完成完整前后端联合运行和 E2E 验证。

## 下一步计划

1. 把向量配置、Embedding 配置和向量索引重建能力产品化：配置持久化、后台任务、进度展示、失败批次记录和重试策略。
2. 按默认 MySQL 配置重新验证账号保存、模型新增、管理员环境保存、连接测试和持久化读取。
3. 将本轮向量检索增强、多轮上下文继承、阶段分布真实运行态聚合、技术文档、测试用例、进度文件和协作看板拆分为合理提交并推送到 GitHub。
4. 请用户基于当前 `http://127.0.0.1:5173/` 继续验证前端工作台体验，重点测试同一会话连续追问和口语化业务对象表达。
5. 请用户刷新 `http://127.0.0.1:5173/` 验证已落地的顶部导航、三栏对话工作台和右侧“后端处理流程”交互；如需微调宽度或显示字段，再继续迭代。
6. 决定是否将当前前端工作台本地改动提交为独立功能提交。
7. 继续推进 Agent 能力：模型驱动结构化意图理解、多轮记录级上下文引用、候选澄清、DAG 计划、多工具循环和 Reflection 自修正。
8. 在混合检索基础上继续扩展更多自然语言统计问法，例如按阶段统计商机数量、按负责人统计商机金额、查询本月新增客户数量。
9. 将分析类能力从当前商机和客户扩展到更多真实云枢业务对象，并补充按时间、负责人、阶段、金额等维度的可控筛选与下钻。
10. 增强多轮澄清上下文：用户补充“就查商机”这类短答时，应能承接上一轮澄清问题并继续执行。
11. 优化阶段/状态分布准确性：在当前轻量样本兜底基础上，优先同步字段级元数据和列表字段配置，评估云枢运行态聚合/筛选接口，减少对详情样本的依赖。
12. 为后端补充根路径或健康检查接口，避免直接访问 `http://127.0.0.1:8080/` 时返回 500。

## 更新规则

- 每个任务完成后必须更新本文件。
- 每次验证、阻塞、恢复、交付前都必须更新本文件。
- 本文件只记录事实进度，不写入密码、Token、Cookie、API Key、私钥或本地敏感配置。
- 每次更新后单独提交并推送 `PROGRESS.md`，让 GitHub 保持最新状态。

- 2026-07-15：完成 OpenClaw/Codex 风格执行活动流改造。实时思考、元数据匹配、云枢 API 调用、请求参数、返回数据、回答生成和任务完成按时间顺序展示，不再压缩成固定四步模板；消息区和右侧栏复用同一组件。
- 2026-07-15：后端新增完整执行事件记录，将 `phase/state/kind/data/elapsedMs` 写入 `AgentResponse.steps` 和助手消息 `metadataJson.executionTimeline`；历史会话刷新后仍可恢复完整过程。新增时间线敏感信息脱敏，避免密码、Token、API Key 等进入执行详情。
- 2026-07-15：修复右侧详情展开撑高中央区域的问题。桌面端两侧栏固定在视口高度内，右侧栏保持 320-360px；响应式断点由 1280px 调整为 1180px，1265px 视口仍为三栏布局。
- 2026-07-15：完成专项验证：后端全量 16 个测试通过；前端 `npm run build` 通过；真实发送“系统有多少商机？”返回真实对象 `int_bu_oppor`、总数 237；后端保留完整执行事件，前端展示层合并为关键活动节点；1265px 桌面和 390px 移动端均无横向溢出，浏览器控制台无错误。
- 2026-07-15：当前服务已启动，前端 `http://127.0.0.1:5173/`，后端 `http://127.0.0.1:8080/`。已知边界：当前事件契约尚未包含生产级 `eventId/callId/parentId`，并行工具调用和复杂重试后续仍需升级为不可变事件日志。
- 2026-07-15：修复执行过程重复展示。完成态隐藏“已收到问题”和“正式回答”等低信息量过渡节点，商机计数从 9 个展示节点压缩为 7 个关键节点；运行中仍保留即时反馈。评审发现“按最近状态文字去重”会误伤多工具和重试事件，已改为只合并同一生命周期 running -> terminal，并仅隐藏被真实 thought/execution 覆盖的四类通用进度。右侧执行详情展开时，消息正文不再同时显示第二份相同时间线。真实页面刷新、历史恢复、390px 响应式和控制台均验证通过，前端生产构建通过；后端原始审计时间线保持完整不变。
- 2026-07-15：完成思考与执行过程纯事件驱动改造。前端不再根据问题关键词或等待秒数预设未来步骤，临时回复以空正文、空步骤开始；后端真实动作开始后才发送 running，拿到结果后更新 completed/fallback/needs_input。SSE 客户端串行消费增量，规则/兜底回答让出浏览器绘制帧，浏览器已观察到 pending 状态下的部分正文。修复“系统现在有多少商机”把“现在”误判为负责人的跨层解析问题，真实复测返回 `int_bu_oppor` 总数 237。后端全量测试、前端构建、真实 SSE 顺序、浏览器逐阶段采样和控制台检查均通过；后端服务已更新并运行在 `8080`。

- 2026-07-02：根据专家评审文档 `docs/technical-design/details/07-intent-action-output-chain-review-2026-07-02.md`，优先落地 P0 正确性与安全性修复。后端修复内容包括：`AgentOrchestrator` 调整意图识别顺序，避免“取消的订单/取消项目金额”等状态统计读请求误入删除链路；删除操作增加目标槽位校验，缺少记录 ID 或明确“第几条”时进入澄清，不再在确认单生成前抛异常；`CloudPivotRuntimeService` 扩展记录序号解析，支持阿拉伯数字和中文数字（一到十及常见十位表达），删除路径无法解析序号时不再默认第 1 条；状态过滤修复“未完成”误命中“已完成”的问题；`AuditService.confirm` 增加 pending、过期、重复确认和执行失败状态校验，避免重复确认二次执行云枢写操作。前端修复内容包括：`ChatView.vue` 根据确认接口返回的 `executed/status/message` 分别展示“已执行”“已确认但未自动执行”“失败/过期/重复处理”等状态，不再无条件提示成功。验证结果：`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示。P1/P2 中的运行态过滤解耦、owner 聚合上限、金额单位换算、关系字段识别和结构化 Planner 重构尚未纳入本轮，需作为后续任务继续推进。

- 2026-07-02：根据用户再次明确的系统主链路要求，完成一次执行逻辑框架修正：目标链路应为“获取用户输入 -> 理解用户意图 -> 从本地元数据库检索云枢实体、数据项、关联关系和业务动作 -> 将用户问题与元数据交给 AI 生成结构化执行步骤 -> 调用云枢 API 获取/操作数据 -> 再把用户问题、元数据、执行路径和查询结果交给大模型总结输出”。本轮定位到当前实现的关键偏差：AI 规划此前只补充 intent/dimension/filter 文本，不能表达可执行 API、字段过滤、指标、分组、排序和确认要求，导致执行层仍大量依赖关键词规则二次判断。已扩展 `IntentPlanningResult`，新增 `apiOperation`、`executionSteps`、`runtimeFilters`、`metricFieldCodes`、`groupByFieldCodes`、`sortFields`、`resultLimit` 和 `requiresConfirmation`；更新 `OpenAiCompatibleModelGateway` 的规划 Prompt，要求模型只能使用本地元数据中的真实字段编码和 API 能力输出结构化执行计划；`AgentOrchestrator` 已优先使用 AI 规划的 `runtimeFilters` 和 `metricFieldCodes` 作为云枢运行态查询参数，并把模型执行计划写入 `runtimeReasoningContext` 与右侧 Think 摘要，规则解析降为兜底。验证结果：`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过。当前仍未完成完整 DAG 执行引擎和运行态服务去规则化，`CloudPivotRuntimeService.summarize(...)` 仍有较多基于用户问题的规则分支，后续需要继续把聚合、分组、排序、详情/列表选择改成由结构化执行计划驱动。

- 2026-07-02：按用户要求“不再让用户做小白鼠”，完成两组真实云枢多轮自测并修复发现的问题。第一组选择商机对象：先问“系统有多少商机？”命中真实 `schemaCode=int_bu_oppor`，返回 237 条；追问“这些商机金额有多少？”能承接商机上下文，AI 执行计划识别金额字段 `Implementation_amount`、`max_pre_sign_dam_yuan`，金额识别从修复前的 4 条提升到 25 条样本；追问“分别处于什么阶段？”修复前继承上下文时字段线索为空，且阶段详情补齐只取 2 条，现已修复 `MetadataExecutionPlanner.inherited(...)`，继承对象时按 schemaCode 回查实体并重新加载数据项、关联关系和 API 能力，同时将阶段补详情样本从 2 条提升到 100 条，复测返回阶段字段 `cust_proj_stage/oppor_status/sales_stage/sequenceStatus`，并按 100 条样本输出未签约 49、暂停 6、已签约 28、停止 15、丢单 2；追问“第一条商机是什么信息？”能返回第一条商机详情。第二组选择客户对象：先问“系统有多少客户？”命中真实 `schemaCode=crm_customer`，返回 8711 条；追问“这些客户每年的客户量情况怎么样？”修复前错误从任意字段抽取年份，输出 1900-2099 伪年度分布，现已限制年度分析只从明确日期/时间/年份字段提取年份，并过滤到 2000 至当前年+1，复测不再输出伪年份；当运行态返回样本缺少可识别日期字段时，系统明确提示无法识别年份字段，不再编造年度结论。验证结果：`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过；`web/` 下 `npm run build` 通过；后端和前端均已启动，`8080/api/metadata/apps` 与 `5173/` 均返回 200。当前仍需继续优化：商机金额和阶段分布仍是样本口径，不是 237 全量口径；客户年度分析需要通过详情补齐或云枢字段配置拿到创建时间后才能给出真实年度分布。

- 2026-07-03：根据用户反馈“UI 交互没有参考现在的 Codex”，完成对话页第一版 Codex 风格改造。前端 `ChatView.vue` 从厚重企业后台卡片布局调整为更接近 Codex 的轻量对话流：右侧执行详情默认收起，让主对话窗口占比更大；助手消息改为无厚边框的自然文本流，用户消息保留右侧浅色气泡；消息复制按钮默认隐藏，悬停/聚焦时显示；思考过程保留在助手消息内联区域，样式更轻；底部输入框改为更接近 Codex 的 sticky composer，发送按钮改为圆形图标按钮；页面自动滚动改为滚动浏览器窗口，而不是内部消息容器。验证结果：`web/` 下 `npm run build` 通过；本地 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。当前仍需后续做真实浏览器截图比对和细节打磨，例如空态、移动端、执行详情展开态和流式输出视觉节奏。

- 2026-07-03：按用户要求启动 2 个架构师对技术方案和代码进行评审，评审结果保存到 `.codex/architect-review-results.csv`。Agent 主链路架构师指出的主要问题：运行态远端过滤成功后仍无条件本地二次过滤，可能把真实 total 覆盖为当前页样本数；负责人筛选优先把聚合查询上限截断为 50 条；模型规划结果可覆盖写操作安全意图，但未校验 `requiresConfirmation` 与 `apiOperation` 一致性；结构化计划尚未成为执行契约，执行层仍有多处关键词推断；非删除写操作会创建确认单但缺少可执行写计划；金额单位“万/万元/亿”解析仍可能错误；模型规划 JSON 缺少统一验证器。前端交互与可观测性架构师指出的主要问题：SSE 流缺少端到端取消，用户断开后后端任务仍可能继续执行并写库；流式请求使用公共 `CompletableFuture.runAsync` 线程池，缺少专用 Executor 和并发控制；进度事件只有 title/status 文本，无法形成稳定状态机和指标；当前仍是完成后伪流式切块，不是真实模型 token 流；前端 SSE 单事件 JSON 解析缺少容错；历史 trace 恢复依赖前端解析审计内部 JSON，展示协议和审计存储强耦合。以上均为评审结论，尚未进入代码修复；后续应按 P1 优先处理过滤/聚合口径、安全意图校验、SSE 取消与专用执行器。

- 2026-07-03：根据用户截图反馈“点击展开执行详情的时候界面错乱”，修复 `ChatView.vue` 的三栏网格布局。根因是 Codex 风格改造时将默认网格右侧列固定成 48px，导致执行详情展开后仍按折叠宽度渲染，按钮、提示和面板内容互相挤压覆盖。现已将默认展开态右侧列恢复为 `minmax(320px, 360px)`，仅在 `.chat-workbench--trace-collapsed` 状态下使用 48px 窄栏；历史会话折叠 + 执行详情展开时也保留右侧可用宽度。验证结果：`web/` 下 `npm run build` 通过；本地 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。

- 2026-07-03：根据用户截图反馈“UI 设计应该是 3 个卡片，看上去比较美观一些”，完成对话页三卡片视觉恢复。前端 `ChatView.vue` 将左侧历史会话、中间主对话、右侧执行详情明确恢复为三个独立卡片：三个区域均使用白色背景、8px 圆角、浅边框和轻阴影；工作台恢复 12px 间距和外侧留白；中间 `chat-shell` 增加边框、圆角和 `overflow:hidden`，避免主对话区与左右区域视觉粘连；右侧执行详情展开态保持 `minmax(320px, 360px)`，折叠态才收窄到 48px，避免再次出现展开时面板挤压错乱。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；本地 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。本轮仅提交并推送 `PROGRESS.md`，界面源码改动暂未提交，等待用户确认视觉效果后再纳入源码提交。

- 2026-07-03：根据用户截图反馈“历史会话刷新按钮和折叠按钮不在同一个区域”，完成左侧历史卡片头部按钮布局修复。根因是 `.conversation-panel__header div` 的通用样式把右侧 `.panel-actions` 也设置成 `display:grid`，覆盖了按钮组原本的 flex 横向排列，导致刷新和折叠两个圆形按钮上下分离。现已将网格样式收窄到标题文本区域 `> div:first-child`，并明确让历史会话和执行详情头部的 `.panel-actions` 保持 `display:flex` 与 `flex-shrink:0`，两个按钮会在标题右侧同一行展示。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；本地 `http://127.0.0.1:5173/` 返回 200，后端 `http://127.0.0.1:8080/api/metadata/apps` 返回 200。

- 2026-07-01：根据用户建议完成“云枢业务对象 API 接口清单元数据化”落地：新增 `cloudpivot_api_endpoints` 表、`CloudPivotApiEndpoint` 实体和仓库，元数据同步时写入接口能力并生成 `api_endpoint` 检索文档；接口清单覆盖集合查询、详情查询、新增/修改、删除、数据项元数据和 Schema 元数据。
- 2026-07-01：新增 `MetadataExecutionPlanner`，在 Agent Think 阶段把用户意图、候选元数据、实体字段、关联关系和 API 接口能力组合成执行计划；右侧处理流程和审计 JSON 已补充执行计划、字段线索、关联线索和接口线索。
- 2026-07-01：修复本轮开发中发现的编码污染问题，恢复 `AgentOrchestrator.java` 与 `CpClawApiTests.java` 到可编译状态，并清理 `MvpCloudPivotConnector.java`、`MetadataService.java`、新增仓库文件的 BOM 和损坏字符串。
- 2026-07-01：更新关键技术文档 `docs/technical-design/details/06-key-technical-strategy.md`，新增“云枢业务对象 API 接口元数据化”章节，详细记录每个接口的输入、输出、数据能力和风险策略。
- 2026-07-01：定向执行 `server/` 下 `mvn -Dtest=CpClawApiTests test`，结果通过：Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；覆盖接口元数据检索、真实业务对象查询、多轮上下文、右侧 Think 执行计划和 API 接口线索。

- 2026-07-01：根据用户要求“云枢元数据这里，把云枢实体对象、实体对象中的数据项、以及关联关系、和实体对象的 API 动作，全部展示出来”，完成元数据模型浏览能力。后端新增 `GET /api/metadata/model`，按应用聚合实体对象，并在实体下返回数据项、关联关系和适用于实体的 API 动作，同时返回全局 API 动作清单；前端 `MetadataView.vue` 已改造为元数据模型浏览页面，支持应用筛选、实体搜索、实体详情 Tab（数据项 / 关联关系 / API 动作）和顶部统计。测试专家补充自动化断言覆盖 `/api/metadata/model`，验证返回 4 个应用、商机实体 `int_bu_oppor`、字段 `opportunityCustomer`、关联目标 `crm_customer`、API 动作 `query_collection`。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 2026-07-01：根据用户补充的云枢业务对象 API 规则，修正 API 动作元数据展示策略。每个业务对象现在按所属应用编码 `AppCode` 和实体编码 `SchemaCode` 动态生成 5 个对象级 API 动作：Create、Delete/{bizObjectId}、GetList、Update、Load/{bizObjectId}；其中新增、更新、删除标记为需用户确认，列表和详情为读操作可直接执行。`cloudpivot_api_endpoints` 继续作为能力模板和检索资料保留，`GET /api/metadata/model` 在实体详情下返回已替换 `AppCode` 与 `SchemaCode` 的真实业务规则路径，例如 `/api/api/runtime/business_rule/zlcsstcrm/int_bu_oppor/GetList`。测试断言已更新，覆盖商机实体下 5 个 API 动作和对象级路径生成规则。
- 2026-07-01：根据用户要求“云枢元数据这里，增加一个应用层，在应用下面才有实体对象”，完成元数据页面层级调整。前端 `MetadataView.vue` 左侧模型浏览由“实体平铺 + 应用筛选”改为“应用 -> 实体对象”的分组结构；应用作为一级节点显示应用名称、应用编码和实体数量，展开后展示该应用下的实体对象，右侧实体详情继续展示数据项、关联关系和 API 动作。实体搜索保留，并支持按应用名称、应用编码、实体名称和实体编码过滤。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方注释和 chunk 体积提示。
- 2026-07-01：根据用户要求“我输入的信息靠右展示，系统的信息靠左展示”，修复对话消息对齐方式。根因是 `.chat-messages` 为 flex column 布局，但用户消息此前使用 `justify-self:end`，该属性对 flex 子项不生效；已改为用户消息 `.message--user { align-self:flex-end }`，CPClaw 和系统消息 `.message--assistant/.message--system { align-self:flex-start }`，并保持用户消息角色文本右对齐。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方注释和 chunk 体积提示。
- 2026-07-01：根据用户截图反馈“API 不是所有的都是 POST 请求”，修正业务对象级 API 动作 HTTP Method。对象级 5 个基础接口现在按云枢规则展示：Create=POST、Delete=DELETE、GetList=POST、Update=PUT、Load=GET；自动化测试新增断言覆盖商机实体下 5 个 API 动作的 method，避免后续退化为统一 POST。技术文档同步新增 HTTP Method 规则，并记录流程生效 Available、流程作废 Cancel 作为后续流程类动作扩展点。
- 2026-07-01：根据用户要求“历史会话支持删除功能”，完成会话删除能力。后端新增 `DELETE /api/conversations/{id}`，删除指定会话及其消息，审计记录暂保留以保证审计链不被物理删除；前端历史会话列表新增删除图标按钮，点击后弹出二次确认，确认后调用删除接口，若删除当前会话则自动清空当前窗口并切换到下一条会话。测试补充覆盖创建待删除会话、写入消息、删除会话、再次访问详情返回失败。验证结果：`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过；`web/` 下 `npm run build` 通过；本地真实接口创建临时会话并执行 `DELETE /api/conversations/{id}` 返回 200。
- 2026-07-01：根据用户要求“系统在回答问题的时候，显示思考时间耗时多长、回答耗时多长”，完成对话耗时展示能力。后端 `AgentResponse` 新增 `thinkingElapsedMs` 和 `answerElapsedMs`，`AgentOrchestrator` 使用 `System.nanoTime()` 统计本轮从 Observe/Think/Plan/元数据匹配到 Act 前的思考耗时，以及 Act/回答生成/Reflect/审计写入到返回前的回答耗时，并写入助手消息 metadata，保证历史消息也能恢复显示。前端 `ChatView.vue` 在 CPClaw 回复下方展示“思考耗时 / 回答耗时”，优先读取接口响应字段，历史消息则从 `metadataJson` 读取。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 2026-07-01：根据用户截图反馈“历史会话删除报错”和“删除按钮鼠标移动上去才显示”，完成会话删除体验修复。前端删除会话前增加会话 ID 校验并对路径参数进行 URL 编码，避免空 ID 请求落到 `/api/conversations` 集合路径触发 `DELETE is not supported`；后端新增 `DELETE /api/conversations` 保护分支，空 ID 删除返回明确的 400 错误“会话ID缺失，无法删除”。历史会话删除按钮默认隐藏，仅在会话项 hover、键盘聚焦或按钮 loading 时显示。验证结果：`web/` 下 `npm run build` 通过；`server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
- 2026-07-01：根据用户要求“每个消息下面支持复制的小图标，点击复制按钮支持将内容进行复制”，完成对话消息复制能力。前端 `ChatView.vue` 在每条有正文的消息底部新增复制图标按钮，用户消息右侧对齐、系统消息左侧对齐；点击时复制该条消息正文，并通过 `navigator.clipboard.writeText` 写入剪贴板，旧环境下自动降级到隐藏 textarea + `document.execCommand('copy')`。消息卡片由按钮调整为可聚焦的 `div role=button`，避免复制按钮嵌套在按钮内导致交互异常。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方注释和 chunk 体积提示。
- 2026-07-02：根据用户要求“顶部导航栏使用深色背景”，完成顶部导航视觉调整。前端 `MainLayout.vue` 将全局顶部导航改为深蓝黑背景，品牌文字和导航文字改为高对比浅色，当前菜单项使用蓝色底部线和半透明高亮，hover/focus 状态增加浅色反馈，保证深色背景下可读性和当前页识别度。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方注释和 chunk 体积提示。
- 2026-07-02：按用户要求启动 2 个测试专家进行完整用例验证。前端测试专家覆盖顶部深色导航、消息复制按钮、历史会话删除 hover/focus 显示、图标按钮可访问名称和前端构建，初测发现纯图标按钮缺少 `aria-label` 风险，已修复刷新历史、删除历史会话、复制消息按钮的可访问名称后复测通过。后端测试专家覆盖会话删除接口、空 ID 删除保护、基础 API 可用性和服务状态；子代理环境中系统 `mvn` 不可用但实时接口复测通过，主控使用指定 Maven 路径补跑 `server/` 下 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过；`http://127.0.0.1:5173/` 返回 200。
- 2026-07-02：根据用户反馈“执行过程也作为思考过程中的一部分，需要按思考内容一样展示出来”，完成等待态思考过程展示优化。前端 `ChatView.vue` 将原本独立的蓝色执行状态行改为消息气泡内部的“思考过程”区块，展示当前阶段、已等待耗时和执行链路说明，统一呈现“理解意图 -> 匹配云枢元数据 -> 调用运行态接口 -> 生成回答”的执行过程，使等待态进度成为 CPClaw 思考内容的一部分。验证结果：`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方注释和 chunk 体积提示。
2026-07-02 本轮根据用户反馈“张三的商机有多少，应结合当前实体对象、数据项和用户问题一起推理，识别为负责人筛选统计”，完成一次框架级意图规划修复，而不是针对单个姓名或单个问题写死规则。核心修复包括：新增通用 `RuntimeQueryFilter`，把用户问题中的人员值映射为结构化运行态筛选条件；`AgentOrchestrator` 会基于当前 `MetadataExecutionPlan.fieldHints()` 生成字段级筛选计划；`CloudPivotRuntimeService` 支持携带筛选计划调用云枢运行态查询，并在云枢条件下推失败时自动降级为详情补齐后的本地字段过滤，避免直接把 404 或错误结果返回用户；`MvpCloudPivotConnector` 扩展查询接口，能够把字段筛选条件写入 `queryCondition` / `filtersNewCondition`，同时保留本地 fallback 过滤能力；`MetadataExecutionPlanner` 已重建为干净 UTF-8/ASCII 安全实现，并增强字段线索选择：当问题包含“负责人/销售/业务员/名下/某人有多少”这类人员筛选语义时，会优先从实体数据项和关联关系 `sourceDataItemId` 中识别负责人字段。真实云枢验证显示，系统现在能够在思考过程中识别商机实体 `int_bu_oppor`，并把负责人字段识别为 `salesman_leader_id`，问题“张峻千有多少商机？”最终返回“张峻千名下商机共 2 条”，不再返回商机总数 237。当前遗留风险：云枢真实环境对我构造的列表过滤条件返回 404，说明 `queryCondition/filtersNewCondition` 的精确格式仍需继续根据云枢接口规范校准；因此本轮虽然正确性已恢复，但真实请求会降级到详情补齐筛选，耗时约 41 秒，性能仍未达标。验证结果：`server/` 下 `-Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；`web/` 下 `npm run build` 通过，仅保留 Vite/Rollup 第三方 PURE 注释和 chunk 体积提示；后端已重新打包并启动，`http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

- 2026-07-02：根据用户反馈“仍未拿着实体对象的数据项结构和用户问题一起推理如何获取数据”，完成一次框架级修复。核心修复包括：`MetadataExecutionPlanner` 将分析型指标字段前置选择，避免“商机来源/商机类型”等普通文本匹配字段挤掉金额字段；修正字段补充阶段先标记后过滤导致关键字段无法入选的问题；`CloudPivotRuntimeService` 在金额汇总中继续携带结构化运行态筛选条件，并在回答中显示筛选口径；`MvpCloudPivotConnector` 改为详情字段覆盖列表字段，避免列表轻量字段的空值或 0 覆盖详情真实值，同时扩展金额控件对象的数值键解析。真实云枢验证“张峻千的商机多少金额？”时，Think 阶段已命中商机实体 `int_bu_oppor`、负责人字段 `salesman_leader_id` 和金额数据项 `预签实施金额(Implementation_amount)`，并按负责人筛选为 4 条记录；当前云枢返回记录中可解析金额仍为 0 或空，系统不再丢失字段计划，但仍需继续校准云枢运行态列表过滤下推格式以解决约 116 秒的慢查询问题。验证结果：`server/` 中 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端重新打包启动后 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。

- 2026-07-02：根据用户确认的推理执行路径，完成 CPClaw 三段式元数据驱动推理链路补齐。技术方案已写入 `docs/technical-design/details/06-key-technical-strategy.md`，新增“元数据驱动的三段式推理执行路径”：第一段从本地云枢元数据库检索实体对象、数据项、关联关系和业务动作；第二段将用户输入与云枢元数据组装 Prompt，调用大模型规划执行路径；第三段调用云枢 API 后，再把用户输入、命中的元数据、执行条件、接口动作和查询结果交给大模型总结输出。代码层面新增 `runtimeReasoningContext`，在 `AgentOrchestrator` 中把用户输入、实体、数据项、关联关系、API 动作、运行态过滤条件、指标字段和模型规划结果传入运行态服务；`CloudPivotRuntimeService` 在模型总结阶段继续传递该上下文；`ModelGateway.analyzeRecords(...)` 增加带 `reasoningContext` 的兼容重载；`OpenAiCompatibleModelGateway` 在总结 Prompt 中追加“Metadata and execution path JSON”，让模型基于真实元数据和查询结果进行二次总结。验证结果：`server/` 中 `mvn -Dtest=CpClawApiTests test` 通过，结果为 Tests run: 1, Failures: 0, Errors: 0, Skipped: 0；后端重新打包启动后 `http://127.0.0.1:8080/api/metadata/apps` 返回 200，前端 `http://127.0.0.1:5173/` 返回 200。当前仍保留规则兜底，模型不可用时系统不会中断查询答案，但大模型可用时会按“用户输入 + 元数据 + 查询结果”进行总结。

- 2026-07-16：根据用户要求“启动服务”，完成本地服务启动与可访问性验证。前端 Vite 已监听 `http://127.0.0.1:5173/`，后端 Spring Boot 已通过已构建 jar 启动并监听 `http://127.0.0.1:8080/`；后端启动时显式带入 MySQL `DATABASE_PASSWORD=h3password`、向量库 `CPCLAW_VECTOR_ENABLED=true`、`CPCLAW_VECTOR_PASSWORD=h3password` 以及阿里云百炼 Embedding 参数，避免使用空数据库密码导致启动失败。验证结果：前端首页返回 200，后端 `/api/settings/models` 返回 200，后端 `/api/conversations` 返回 200。

- 2026-07-16：根据用户要求“对话 UI 参考本地 ChatUI 原型，顶部导航栏仍需要保留，右侧执行详情不需要”，完成对话页布局和视觉调整。前端 `ChatView.vue` 从三栏工作台调整为左侧历史会话 + 中央对话主窗口两栏结构，移除右侧独立执行详情栏；执行过程保留在每条 CPClaw 回复内部的“思考与执行过程”折叠块中。对话区域参考 ChatUI 风格增加头像、轻量气泡、消息内 thinking block、固定底部输入框，输入框桌面端居中 820px、移动端自适应。文档同步更新 `docs/product-design/details/01-requirements.md` 和 `docs/technical-design/details/06-key-technical-strategy.md`。验证结果：`web/` 下 `npm run build` 通过；浏览器自动化验证顶部导航存在、右侧 `.trace-panel` 数量为 0、桌面和移动端均无水平溢出、输入框固定在底部。

- 2026-07-16：根据用户反馈“当前 UI 仍不满意，先设计 UI 界面确认，参考之前发的 HTML”，暂停继续改业务页面，新增静态确认原型 `design/chat-ui-v2-prototype.html`，并导出桌面和移动端预览图 `design/chat-ui-v2-desktop.png`、`design/chat-ui-v2-mobile.png`。原型保留顶部深色导航，采用左侧历史会话 + 中央对话主窗口两栏布局，取消右侧独立执行详情栏，中央空状态增加 3 个高频问题入口，底部输入框固定浮层且桌面宽度约 820px。同步更新 `docs/product-design/details/01-requirements.md` 和 `docs/technical-design/details/06-key-technical-strategy.md`，明确 UI v2 确认前以该原型为设计基准。验证结果：浏览器渲染检查桌面和移动端均无水平溢出。

- 2026-07-16：根据用户补充的 ChatUI 标注参考图，继续修订 `design/chat-ui-v2-prototype.html`。本轮原型新增顶部右侧消耗信息与主题切换入口、会话标题下划线对齐、输入框内部附件与发送按钮、输入框底部模型选择与思考深度配置；重新导出 `design/chat-ui-v2-desktop.png` 和 `design/chat-ui-v2-mobile.png`。同步更新产品和技术文档，明确这些控件在实现阶段必须接入现有模型配置、思考开关和附件上传逻辑，不能只做静态装饰。验证结果：浏览器渲染检查桌面和移动端均无水平溢出。

- 2026-07-16：根据用户最新反馈“顶部导航栏也不需要了，把用户登录信息和设置信息都参考图放置”，继续修订 UI v2 原型。`design/chat-ui-v2-prototype.html` 已移除顶部全局导航栏，品牌、菜单折叠入口、历史会话、用户登录信息和系统设置入口统一放入左侧栏；主区域顶部只保留当前会话标题、消耗信息、主题切换、模型选择和思考模式控制。重新导出 `design/chat-ui-v2-desktop.png` 和 `design/chat-ui-v2-mobile.png`，并同步更新产品和技术文档。验证结果：浏览器渲染检查顶部 `.top-nav` 数量为 0，桌面和移动端均无水平溢出。

- 2026-07-16：按用户指定的 `frontend-design-pro` 流程和本地 `chat-ui/prototype.html` 基准，完成 UI v2 确认原型精修。页面严格收敛为左侧侧栏 + 右侧对话主区两部分：左侧包含 Logo、新建对话、历史对话、用户身份和系统设置；右侧顶部只保留会话名称、Token 消耗与深浅主题切换，移除顶部模型和思考模式配置；模型选择、思考深度、附件上传和发送全部归入底部输入框，输入框内移除“新建会话”。原型补充浅色默认/深色记忆、模型与思考深度下拉、附件预览、历史选择与删除、移动端侧栏抽屉等交互，并统一低对比背景、1px 边界、卡片 8px 圆角和图标可访问名称。桌面预览已重新导出；移动端根据 390px 截图发现标题组和输入工具栏存在宽度竞争后，已改为弹性收缩布局。产品与技术文档已同步到最新确认口径；正式 Vue 页面尚未改造，等待用户确认原型。

- 2026-07-16：根据用户标注反馈继续精修 UI v2 原型顶部和左侧底部。问题根因是左侧头部使用 72px、右侧头部使用 64px 两套独立高度，导致分割线天然错位且顶部显厚；本轮将左右顶部统一改为共享 `--header-height: 56px`，同时把左侧用户身份区压缩为 60px，并等比例缩小 Logo、头像、字体、间距和内边距。产品与技术文档已补充共享高度变量和精确对齐约束；正式 Vue 页面仍等待原型确认后实施。
