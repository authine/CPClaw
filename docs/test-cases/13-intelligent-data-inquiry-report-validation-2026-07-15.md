# 智能问数与咨询式报告验证记录

- 日期：2026-07-15
- 测试角色：QA-A 数据口径测试专员、QA-B 多轮与交互测试专员
- 测试范围：元数据驱动多实体规划、时间和人员口径、确定性指标、图表协议、流式输出、关联追问

## 1. 验收用例

| ID | 优先级 | 输入/场景 | 预期结果 |
| --- | --- | --- | --- |
| IQ-001 | P0 | `2024 年上半年商机情况怎么样？` | 从真实元数据定位商机，并只选择有真实关系支撑的线索、合同等高相关对象；输出 KPI、转化漏斗、阶段分布、风险和建议。 |
| IQ-002 | P0 | 主对象存在 `createdTime`，记录跨越上下半年 | 上半年报告只统计 `[01-01, 07-01)` 范围内记录。 |
| IQ-003 | P0 | `2024 年上半年我的商机情况怎么样？`，账号可在负责人结构值中匹配 | 所有参与指标的对象使用相同期间和当前账号口径。 |
| IQ-004 | P0 | 同上，但账号无法映射负责人 | 不返回全员数据冒充个人结果；指标为可验证空结果并输出身份警告。 |
| IQ-005 | P0 | 商机关联线索，合同关联商机 | 漏斗顺序为线索→商机→合同，转化率由程序计算。 |
| IQ-006 | P0 | 关联关系不存在 | 不生成对应实体、漏斗阶段或虚构指标。 |
| IQ-007 | P0 | `每年的客户量情况怎么样？` | 保留原有单对象年度趋势路径，不错误升级为跨实体报告。 |
| IQ-008 | P0 | 模型流式总结成功 | `thought/execution` 先反馈，正式正文逐 token 输出，最后返回 `final` 和 `insightReport`。 |
| IQ-009 | P0 | 模型超时或输出不完整 | 发送 `answer_reset`，清除半截模型正文，改用已核验指标流式生成稳定报告。 |
| IQ-010 | P0 | 刷新历史会话 | 从 `metadataJson.insightReport` 恢复 KPI、图表、风险和追问。 |
| IQ-011 | P0 | 点击关联追问 | 在同一会话中作为新用户问题发送，继续走元数据和云枢真实查询。 |
| IQ-012 | P0 | SSE 在 `final` 前断开 | 前端报错，不把半截回答当作成功消息。 |
| IQ-013 | P1 | 云枢总数大于本次拉取记录数 | 报告显示截断警告，不宣称图表代表全量。 |
| IQ-014 | P1 | 没有可靠期间字段 | 该实体不参与期间指标，并显示口径警告。 |
| IQ-015 | P1 | 金额字段存在空值 | 金额合计仅计算有效值，并输出数据质量风险。 |
| IQ-016 | P0 | 线索、商机、合同集合混入同期但无关联记录 | 只统计关联字段 ObjectId 能验证的转化和下游结果，不用同期总数相除。 |
| IQ-017 | P0 | 金额合计、阶段分布等对象总数超过首屏 | 分页拉取完整列表，聚合记录上限 20,000；字段覆盖不足时明确显示已识别口径。 |
| IQ-018 | P0 | 模型流有正文但缺少 `[DONE]` 和 `finish_reason` | 判定模型流不完整并回退，不发送成功语义的半截模型答案。 |
| IQ-019 | P0 | 提交问题后尚未收到后端执行事件 | 只显示“正在运行”和耗时；步骤列表为空，不按关键词或等待秒数预设未来步骤。 |
| IQ-020 | P0 | `系统现在有多少商机？` | “现在”作为时间语气词处理，不得生成负责人筛选；返回真实商机总数。 |
| IQ-021 | P0 | 短规则回答的 SSE 增量连续快速到达 | 前端串行消费 `answer_delta`，至少出现一次 pending 状态下的部分正文，再由 `final` 替换为持久化完整消息。 |
| IQ-022 | P0 | 商机对象存在“销售阶段”字段，阶段值为线索、E、D、C、B、A | 图表类型为阶段漏斗，顺序按业务推进排列，不按数量倒序；累计序列单调递减，当前阶段存量单独保留。 |
| IQ-023 | P0 | 跨对象链路数量为 `786→103→237` | 因数值非单调，不得展示为转化漏斗；自动选择条形对比并说明避免误读为转化率。 |
| IQ-024 | P1 | 普通“客户状态”只有正常、停用、待审核等无序值 | 选择环形构成图，不错误识别为阶段漏斗。 |
| IQ-025 | P1 | 按月新增数据 | 选择折线图并按自然月份顺序呈现。 |
| IQ-026 | P0 | 数据项 `rawJson` 包含选项顺序 | 优先使用元数据选项顺序；只对未定义选项使用业务语义兜底。 |

## 2. 自动化结果

### 后端

- `InsightReportServiceTests`：3 个用例通过。
- `InsightVisualizationPlannerTests`：5 个用例通过，覆盖商机阶段漏斗、元数据选项顺序、无序状态构成、非单调链路降级和时间序列折线。
- 覆盖真实关系形成线索→商机→合同漏斗。
- 覆盖 2024 年上半年时间过滤。
- 覆盖金额、阶段、签约、关联追问结构。
- 覆盖当前账号无法匹配时不返回全员数据。
- 覆盖云枢拒绝期间条件后读取完整列表并本地过滤。
- 覆盖通过关联字段 ObjectId 计算线索关联数和合同关联数。
- `OpenAiCompatibleModelGatewayStreamTests`：4 个用例通过，覆盖完整流、提前 EOF、`finish_reason=length` 截断和流式 usage。
- 后端全量测试：24 个测试，0 失败，0 错误。
- `CpClawApiTests` 回归通过，确认年度客户趋势等原有快速路径未被报告模式截获。

### 前端

- TypeScript 类型检查通过。
- Vite 生产构建通过。
- 报告组件支持 KPI、条形图、环形图、漏斗图、数据警告、数据来源和可点击追问。
- 流式客户端增加 `final` 完整性校验。
- 结构化报告不再重复渲染 Markdown 已包含的核心结论、风险和建议章节。
- 桌面与 390px 移动视口均验证报告无横向溢出，页面控制台无错误。

## 3. 尚需真实环境验证

- 真实云枢账号到人员字段的映射成功率。
- `sql_line → int_bu_oppor → contract_quotation` 在真实运行态相同期间的字段完整性。
- 超过 20,000 条记录时的服务端聚合方案。
- 多个金额或日期字段并存时，大模型规划与确定性字段评分是否选中正确业务口径。
- 真实模型长报告流式响应在 120 秒 SSE 超时内的稳定性。

上述项目是已知限制，不得在未验证前标记为生产完成。

## 4. 真实环境复测结果

- 宽泛报告：`2025年上半年商机整体经营情况怎么样？`
  - 上半年商机 147 条。
  - 可识别金额 147/147 条，合计 3973.32 万元。
  - 已签约 48 条。
  - 最终复测耗时约 52.6 秒，思考、规划、逐对象查询和模型正文均收到独立 SSE 事件。
  - 正式回答 `reset=0`、`mode=model`，模型正文 962 个中文字符，结尾完整；未回退规则报告，也未发生长度截断。
- 同一会话追问：`重新按全部商机计算，其中金额合计是多少？`
  - 继承商机对象 `int_bu_oppor`。
  - 总数 237 条，可识别金额 237 条。
  - 金额合计 58,069,016.67，未再退化为前 100 条样本。
- 同一会话追问：`这些商机分别处于什么阶段？`
  - 未签约 135、暂停 13、已签约 65、停止 20、丢单 4，合计 237 条。
- 服务状态：前端 `5173`、后端 `8080` 均可访问；验证结束后已删除 QA 临时会话。

## 5. Agent 活动流专项回归

- 自动化：后端全量 16 个测试通过，覆盖真实时间线响应、历史消息持久化和敏感信息脱敏；前端 `npm run build` 通过。
- 真实对话：发送“系统有多少商机？”后，实时区域先后出现问题接收、上下文理解、执行规划、元数据匹配、云枢接口调用、数据返回、正式回答、校验和任务完成节点。
- 数据展示：真实对象 `int_bu_oppor`，接口 `/api/api/runtime/query/listSkipQueryListV2`，总数 237、返回 1 条均进入对应执行节点。
- 完成态：后端仍保留完整原始事件，前端展示层仅合并同一运行态到完成态的生命周期，并隐藏已有真实 thought/execution 对应的四类通用进度（理解问题、规划路径、查询数据、整理回答）；不再按最近状态文字启发式删除事件。“系统有多少商机？”最终展示 7 个关键节点：理解用户问题、规划执行路径、匹配云枢元数据、调用云枢运行态接口、云枢数据返回、校验执行结果、任务完成。
- 重复展示：执行详情右侧栏展开时，消息正文内不再同时展示第二份相同时间线；右侧栏收起后，消息内仍可展开查看。刷新历史会话后，7 个关键节点仍从 `metadataJson.executionTimeline` 恢复。
- 布局：1265px 桌面视口右侧栏宽 360px、高 618px，不再落到整行下方或撑高中央消息区；无横向溢出。
- 移动端：390px 视口下聊天区优先显示，活动时间线和数据详情无横向溢出。
- 浏览器控制台：0 条错误日志。
- 前端生产构建再次通过；仅有既有第三方 PURE 注释和 chunk 体积提示，无类型或编译错误。

## 6. 真实事件驱动与可见流式专项回归

- 删除前端问题关键词画像和按秒数推测阶段逻辑；临时助手消息创建时 `content=''`、`steps=[]`。
- 后端删除会话保存、固定理解、固定规划、固定查询和固定整理等通用 progress 副本；真实动作改为 `running -> completed/fallback/needs_input` 生命周期事件。
- 真实 SSE“系统现在有多少商机？”共收到 14 个事件、0 个 `progress` 事件；顺序为理解、规划、元数据匹配、接口调用、数据返回、`answer_start`、2 个 `answer_delta`、`answer_end`、校验和 `final`。
- 浏览器实时采样：252ms 时只出现“理解用户问题、规划执行路径”；4.96s 接口真正开始后才追加元数据和接口节点；5.44s 在消息仍为 pending 时已显示第一段 24 字正文，证明不是等待 `final` 后一次性渲染。
- 最终结果：真实商机对象 `int_bu_oppor`，总数 237；“现在”未被解析为负责人筛选。接口调用节点最终状态为 completed，数据返回节点展示总数 237、返回 1 条。
- 自动化：后端全量测试通过；`CpClawApiTests` 新增“系统现在有多少商机”回归和运行态过滤依据校验；前端生产构建通过；浏览器控制台无错误。

## 7. 思考过程与正式回答层级专项回归

- 历史完成消息：每条 CPClaw 回复均在 Markdown 正文上方保留“思考与执行过程”，默认关闭，点击后可恢复完整时间线。
- 实时消息：发送“系统现在有多少商机？”后，250ms 采样显示 pending 消息子节点顺序为角色、实时思考区、Markdown 正文；此时思考区已有真实理解和规划事件，正文尚为空。
- 完成切换：收到 `final` 后，实时思考区转换为关闭的折叠区，正文仍位于其下方；真实结果为 `int_bu_oppor` 总数 237。
- 多轮追问：继续发送“这些商机按阶段分布怎么样？”，完成后返回未签约 135、暂停 13、已签约 65、停止 20、丢单 4；折叠区位于回答上方，展开后回答从折叠区底部继续自然排列，无覆盖或顺序反转。
- 响应式：390px 视口下页面 `scrollWidth` 等于可视宽度，消息顺序不变，无横向溢出；浏览器控制台 0 条错误。
